package syneroym-test:introducer@0.1.0;

interface greet {
    /// Runs the application and returns a result string.
    greet: func(name: string) -> string;
}

world introducer {
    export greet;
}
