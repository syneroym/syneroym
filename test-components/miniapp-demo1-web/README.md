# Standard web app
Example demonstrating different aspects of standard Web application: Static and dynamic pages, REST calls, Large upload/downloads, real-time websockets. Kind of test, that all kinds of standard features can be run in p2p mode too.

- To build the html-css-js UI, go to the client folder and run `npm run build`
- Rust code builds with the workspace's `cargo build`
