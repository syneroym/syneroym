[package]
name = "syneroym-test-proxy"
version = "0.1.0"
edition = "2024"

[dependencies]
wit-bindgen = "0.55.0"

[lib]
crate-type = ["cdylib"]

[package.metadata.component.target.dependencies]
"syneroym:proxy" = { path = "wit/deps/proxy" }
