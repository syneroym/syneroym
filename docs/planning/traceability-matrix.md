# Traceability Matrix

This document maps the requirements from the System Requirements Specification (`system-requirements-spec.md`) and related documents to their target milestones, current implementation status, and acceptance evidence.

## Core Requirements

| Requirement ID | Description | Target Milestone | Status | Acceptance Evidence |
| --- | --- | --- | --- | --- |
| `[PRD-AUT]` | Provider identity, data, policy, and operator choice remain under provider control. | TBD | Pending | Delegation-revocation and operator-migration journeys. |
| `[PRD-CUX]` | Consumers complete the reference journey without understanding hosting or federation. | M6 | Pending | Moderated task-success test and accessibility audit. |
| `[PRD-FED]` | Independent implementations interoperate without a mandatory central data-plane or authority. | M8 | Pending | Two-node federation and bootstrap-outage tests. |
| `[PRD-OFF]` | Safe workflows remain intelligible and converge after disconnection; unsafe retries fail explicitly. | M5 | Pending | Fault-injection, idempotency, and state-model tests. |
| `[PRD-POR]` | Participants can export, verify, and restore in-scope identity-linked data through versioned open formats. | TBD | Pending | Clean-node export/import drill and cross-version fixtures. |
| `[PRD-TRU]` | Trust evidence is sourced, scoped, fresh, explainable, and correctable; uncertainty remains visible. | M8 | Pending | Trust-display, revocation, omission, and abuse cases. |
| `[PRD-OPS]` | A non-specialist can install or join, understand health, recover, update, and exit within the declared operating profile. | M2 | Complete | Operator-facing guided install, plain-language `/v1/info` health endpoint, and operational observability smoke tests passing. |
| `[PRD-EXT]` | Third-party SynApps can declare capabilities and pass public compatibility tests. | M5 | Pending | Package inspection and protocol conformance suite. |
| `[PRD-SAF]` | Consent, data lifecycle, moderation boundaries, and responsible parties are explicit throughout a transaction. | TBD | Pending | Retargeted from M4 (2026-07-13): M4's FDAE/UCAN provides the enforcement *mechanism*, but the policy-authoring UX, dispute/report, and deletion scenarios belong with a consumer-facing surface (Chat/Hub, M6+) that does not yet exist. See M04A Decision Register A.1. |

## Substrate Capabilities

| Requirement ID | Description | Target Milestone | Status | Acceptance Evidence |
| --- | --- | --- | --- | --- |
| Requirement ID | Description | Target Milestone | Status | Acceptance Evidence |
| --- | --- | --- | --- | --- |
| `[TOP-PRM]` | Core Primitives & Overlay | M1 | Complete | Domain models implemented in [models.rs](file:///Users/pari/gitSyneroym/syneroym/crates/app_orchestration/src/models.rs); compiled manifests deployable via [service.rs](file:///Users/pari/gitSyneroym/syneroym/crates/control_plane/src/service.rs). |
| `[TOP-ADR]` | Service Addressing | M1 | Complete | Logical service references, resolver topologies, and rendezvous hashing implemented in [resolver.rs](file:///Users/pari/gitSyneroym/syneroym/crates/app_orchestration/src/resolver.rs) and [models.rs](file:///Users/pari/gitSyneroym/syneroym/crates/app_orchestration/src/models.rs). |
| `[TOP-REG]` | Registries (App & Endpoint) | M1 | Complete | `AppRegistry` and `StaticInventory` implemented in [resolver.rs](file:///Users/pari/gitSyneroym/syneroym/crates/app_orchestration/src/resolver.rs); DHT registry Master Anchor endpoints implemented in [dht_registry.rs](file:///Users/pari/gitSyneroym/syneroym/crates/core/src/dht_registry.rs) and [registry.rs](file:///Users/pari/gitSyneroym/syneroym/crates/community_registry/src/registry.rs). |
| `[TOP-DSC]` | Discovery Mechanisms | M1 | Complete | Top-level `resolve_master_anchor` implementation resolving master keys to authorized temporary keys in [dht_registry.rs](file:///Users/pari/gitSyneroym/syneroym/crates/core/src/dht_registry.rs); logical resolver endpoint caching. |
| `[LFC-MGT]` | Standalone `roymctl` Deployment & Manifest parsing | M1 | Complete | Parse logic in [models.rs](file:///Users/pari/gitSyneroym/syneroym/crates/app_orchestration/src/models.rs), reconcile loops in [reconcile.rs](file:///Users/pari/gitSyneroym/syneroym/crates/app_orchestration/src/reconcile.rs) and sqlite journal storage in [journal.rs](file:///Users/pari/gitSyneroym/syneroym/crates/app_orchestration/src/journal.rs), CLI integration in [app.rs](file:///Users/pari/gitSyneroym/syneroym/apps/roymctl/src/commands/app.rs). |
| `[LFC-VER]` | Manifest versioning | M1 | Complete | Semver validation on manifests implemented in [models.rs](file:///Users/pari/gitSyneroym/syneroym/crates/app_orchestration/src/models.rs). |
| `[TOP-ROB]` | Network & Connection Robustness | M2 | Complete | Retry with exponential backoff, reactive eviction, and QUIC idle timeout implemented. Verified by e2e smoke tests. |
| `[FND-IDT]` | Cryptographic Identity Primitives | M2 | Complete | Master Key to Temporary Key delegation certificates and basic handshake authorization implemented. |
| `[FND-DEP]` | Deployment / Operations | M2 | Complete | Native TLS (rustls), release pipeline, Dockerfile, and smoke tests against live coordinator implemented. |
| `[FND-SEC]` | Substrate Security | M2 | Complete | Wasmtime fuel metering quotas, connection caps, and `mlock` memory protection for keys implemented. |
| `[FND-SEC]` (M3A: storage encryption) | Envelope encryption (DEK/KEK), secret vault, production-mode enforcement | M3 (M3A) | Complete | SQLCipher envelope encryption and `mlock`'d KEK in [key_store.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_keystore/src/key_store.rs); per-service encrypted DB isolation and `_vault` table in [sqlite.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/src/sqlite.rs); `syneroym:vault/reveal` host function in [engine.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/src/engine.rs). DEK-never-plaintext-on-disk verified by a real on-disk-file regression test, `test_dek_never_plaintext_on_disk` in [key_store.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_keystore/src/key_store.rs). Production insecure-warning verified by `test_insecure_mode_warning` in [sqlite.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/src/sqlite.rs). **Descoped (recorded, not a gap):** a dedicated test for KEK rotation concurrent with an in-flight request was descoped at closeout — the property holds by construction (`rotate_kek` only touches `substrate.db`'s `dek_store` table; no request re-reads it mid-flight) — see `docs/planning/milestones/M03-sss/status.md` closeout notes. |
| `[PLT-DAT]` (M3A: structured data) | Per-service encrypted SQLite, CRUD/batch/query WIT surface, MongoDB-style filters, schema lifecycle hooks | M3 (M3A) | Complete | `syneroym:data-layer` WIT in [data-layer.wit](file:///Users/pari/gitSyneroym/syneroym/crates/wit_interfaces/wit/data-layer/data-layer.wit); filter compiler in [filter.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/src/filter.rs); CRUD/DDL/lifecycle-hook host functions in [sqlite.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/src/sqlite.rs) and [engine.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/src/engine.rs); integration coverage in [data_layer_integration.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/tests/data_layer_integration.rs) and [lifecycle_hooks.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/tests/lifecycle_hooks.rs). All CRUD/query/batch-mutate performance budgets verified via `criterion` (`data_layer_bench.rs`). |
| `[PLT-DAT]` (M3B: blob object service) | Content-addressed blob storage over `object_store`, DEK-encrypted at rest, signed URLs | M3 (M3B, Slice 5) | Complete | `syneroym:blob-store` WIT (resource-based streaming + one-shot) in [blob-store.wit](file:///Users/pari/gitSyneroym/syneroym/crates/wit_interfaces/wit/blob-store/blob-store.wit); `ObjectStoreBlobProvider` in [object_store_impl.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_blob/src/object_store_impl.rs); segmented AEAD encryption and signed-URL HMAC in [crypto.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_blob/src/crypto.rs); integration coverage in [blob_store_integration.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/tests/blob_store_integration.rs). `put-blob`/`get-blob` (1 MB, local backend) performance budgets verified via `criterion` (`blob_bench.rs`). Live HTTP `GET` endpoint for `signed-url()` (the deferral this row previously recorded) shipped in M3B Slice 7: `GET /blobs/{hash}?svc=&exp=&sig=` in [http.rs](file:///Users/pari/gitSyneroym/syneroym/crates/router/src/route_handler/http.rs), end-to-end coverage in [http_passthrough_e2e.rs](file:///Users/pari/gitSyneroym/syneroym/crates/substrate/tests/http_passthrough_e2e.rs). |
| `[FND-CFG]` | Versioned configuration delivery, schema validation, generation pinning, **deploy-time artifact delivery (inline vs. host-side), Podman file-mount fallback** | M3 (M3A, Slice 4); artifact delivery in the 2026-07-22 interstitial | In Progress | **Shipped (M3A Slice 4):** `syneroym:app-config` WIT and `config_generations` table in [sqlite.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/src/sqlite.rs); host function and generation-pinning-per-invocation in [engine.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/src/engine.rs) (`test_config_get_and_get_section`, `test_config_isolation_and_generation_pinning`); JSON Schema deploy-time validation in [service.rs](file:///Users/pari/gitSyneroym/syneroym/crates/control_plane/src/service.rs); Podman env-var flattening in [engine.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_podman/src/engine.rs). `config/get` performance budget verified via `criterion` (`security_config_bench.rs`). **Open:** this row previously read Complete while scoped only to the three items above — the M3A Slice 4 scope line (`docs/planning/milestones/M03-sss/task.md:46`) also included "Podman env-var **and file-mount** fallback," and only the env-var half shipped. Additionally, `schema_path` and `fdae_policy_path` are substrate-host paths with no upload path, so neither is deployable through the API. Both closed by the `[FND-CFG]` interstitial — [ADR-0019](../decisions/0019-deploy-time-artifact-delivery.md), [fnd-cfg-artifact-delivery-plan.md](fnd-cfg-artifact-delivery-plan.md). |
| `[PLT-DAP]` | Pluggable `StorageProvider`/`ServiceStore` trait foundations | M3 (M3A, Slice 2A) | Complete (foundations only) | Traits in [traits.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/src/traits.rs). Logical data-service routing across nodes is explicitly deferred to M5 — not this milestone's scope. |
| `[PLT-DAP-04]` | Decentralized Pub/Sub (MQTT API) | M3 (M3B messaging half) | Complete | Embedded `rumqttd` broker (`MqttBroker`) in [lib.rs](file:///Users/pari/gitSyneroym/syneroym/crates/mqtt_broker/src/lib.rs); `syneroym:messaging` WIT `host-api` (publish/subscribe/unsubscribe) in [messaging.wit](file:///Users/pari/gitSyneroym/syneroym/crates/wit_interfaces/wit/messaging/messaging.wit); native and WASM-guest dispatch in [synsvc_native.rs](file:///Users/pari/gitSyneroym/syneroym/crates/control_plane/src/synsvc_native.rs) and [engine.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/src/engine.rs); shipped as Slice 6A, `docs/planning/milestones/M03B-messaging/task.md`. HTTP-facing `POST`/SSE `GET` bridge onto `publish`/`subscribe` shipped in Slice 7: [http.rs](file:///Users/pari/gitSyneroym/syneroym/crates/router/src/route_handler/http.rs), coverage in [http_passthrough_e2e.rs](file:///Users/pari/gitSyneroym/syneroym/crates/substrate/tests/http_passthrough_e2e.rs). |
| `[PLT-DAP-06]` | Generic Bidirectional Streaming | M3 (M3C) | Complete | `syneroym:messaging` `stream-types` interface (`stream-cursor`/`stream-sink` guest-exported resources), `register-stream-protocol`/`handle-stream-request`/`accept-stream-upload` in [messaging.wit](file:///Users/pari/gitSyneroym/syneroym/crates/wit_interfaces/wit/messaging/messaging.wit); host-side QUIC stream acceptance/routing per [0014-quic-stream-protocol-routing.md](file:///Users/pari/gitSyneroym/syneroym/docs/decisions/0014-quic-stream-protocol-routing.md); shipped as Slice 6B, `docs/planning/milestones/M03B-messaging/task.md`. HTTP-facing chunked-upload bridge onto `accept-stream-upload`/`stream-sink` shipped in Slice 7: [http.rs](file:///Users/pari/gitSyneroym/syneroym/crates/router/src/route_handler/http.rs), coverage in [http_passthrough_e2e.rs](file:///Users/pari/gitSyneroym/syneroym/crates/substrate/tests/http_passthrough_e2e.rs). |
| `[PLT-DAT]` (M4A: Universal Proxy) | Typed inter-component / cross-node RPC over **JSON-RPC** + full WIT⇄JSON value conversion; wRPC binary wire deferred | M4 (M4A) | Complete | `docs/planning/milestones/M04A-proxy-and-auth-foundation/task.md` (Slices A0′, A1) — see `status.md`'s A0′/A1 sections; cross-node proof in [multi_hop_relay.rs](file:///Users/pari/gitSyneroym/syneroym/crates/coordinator_iroh/tests/multi_hop_relay.rs) (`test_cross_node_proxy_call`). `AggregationPipeline` + `query-raw` data-layer extensions also M4A (Slices B4, B5, both Complete) — see `status.md`'s B4/B5 sections. |
| `[FND-IAM]` (M4A: auth foundation) | UCAN context extraction/normalization at ingress, verified caller-identity threading through native dispatch, Admin UCAN capability, grant-layer access control (owner-rooted trust, deploy authorization) | M4 (M4A) | Complete | `docs/planning/milestones/M04A-proxy-and-auth-foundation/task.md` (Slices B0, B1, B7a, B7b) — see `status.md`'s B0/B1/B7a/B7b sections. Closes the M3B/M3C native-dispatch authentication gap (B0); UCAN chain verification and `SessionContext` normalization in [crates/ucan](file:///Users/pari/gitSyneroym/syneroym/crates/ucan) (B1); service-ownership attribution and the Tier-1 `orchestrator/{deploy,undeploy,status}` capability gate, including owner-rooted trust (ADR-0015 A6), in [route_handler/io.rs](file:///Users/pari/gitSyneroym/syneroym/crates/router/src/route_handler/io.rs) and [service/orchestration.rs](file:///Users/pari/gitSyneroym/syneroym/crates/control_plane/src/service/orchestration.rs) (B7a/B7b). |
| `[FND-SEC]` (M4A: per-app KEK) | Per-SynApp-Instance KEK narrowing (D-03-01 follow-on) | M4 (M4A) | Complete (derived only) | `docs/planning/milestones/M04A-proxy-and-auth-foundation/task.md` (Slice B6) — see `status.md`'s B6 section. Ships the **derived-KEK model ("Model A")**: `derive_instance_kek` (HKDF-SHA256, scoped by `service_id`) in [key_store.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_keystore/src/key_store.rs); cross-instance cryptographic isolation and the re-wrap path proven by `cross_instance_kek_isolation`/`rotate_kek_preserves_per_instance_deks` in the same file. **DEFERRED, blocks multi-tenant production:** IAM-gated per-instance/per-service KEK *provisioning* (a distinct, externally-injected KEK per instance, "Model B") — the property that actually isolates tenants against a party holding the master KEK or substrate RAM — has no provisioning channel or consumer in M04A and is **not** delivered; see [ADR-0006](../decisions/0006-sqlite-encryption-sqlcipher.md)'s Amendments section, whose multi-tenant-not-secure-until-per-instance-KEK caveat stays in force, retargeted at Model B. |
| `[FND-IAM]` (M4B: FDAE) | Data-aware authorization — declarative ReBAC compiled to SQLite (RLS/CLS pushdown sieve), 4-stage hybrid pipeline, federated cross-service fetch, stage-4 WASM ABAC, write-side Mode-A authorization | M4 (M4B) | Complete (Slices B2, B3, B3.5-fdae, B4-fdae, B5-fdae complete) | `docs/planning/milestones/M04B-fdae-policy/task.md` (Slices B2, B3, B3.5-fdae, B4-fdae, B5-fdae) — see `status.md`'s Phase 1-5 and Slice B3.5-fdae/B4-fdae/B5-fdae sections. **Delivered (B2):** the policy model + JSON-Schema-validated ReBAC→SQL compiler (Mode A point-in-time, Mode B relational filter; `WHERE EXISTS`/cycle-guarded `WITH RECURSIVE`; RLS row-pruning + CLS column masking; `strict:`/policy-internal default-deny) in [crates/fdae](file:///Users/pari/gitSyneroym/syneroym/crates/fdae); threaded into `get`/`query`/`aggregate`/`delete_many`/`check_access` with a `sqlite3_progress_handler` watchdog default-deny in [sqlite.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/src/sqlite.rs); the `check-access` WIT host function and host-side CLS field-stripping in [host_capabilities.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/src/host_capabilities.rs); manifest/deploy/persist plumbing and native-dispatch enforcement in [orchestration.rs](file:///Users/pari/gitSyneroym/syneroym/crates/control_plane/src/service/orchestration.rs); the ADR-0017 §9 decision trace (`fdae::DecisionTrace`, emitted via `tracing`) in [trace.rs](file:///Users/pari/gitSyneroym/syneroym/crates/fdae/src/trace.rs); the `< 25 ms p99` pushdown-query performance budget verified via `criterion` in [fdae_bench.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/benches/fdae_bench.rs) (~80 µs measured, single-hop, 100 records); reference scenario step 22 (transparent Mode-B filtering) proven end to end for a router-verified caller in [native_dispatch_identity.rs](file:///Users/pari/gitSyneroym/syneroym/crates/router/tests/native_dispatch_identity.rs) (`native_fdae_policy_row_filters_and_masks_for_two_distinct_verified_callers`). **Delivered (B3):** `anchor_did` signed stamp + propagation invariant and the `anchor` path-terminal (ADR-0015 A5) in [crates/ucan](file:///Users/pari/gitSyneroym/syneroym/crates/ucan)/[crates/fdae](file:///Users/pari/gitSyneroym/syneroym/crates/fdae); the two-phase compile (`plan_read`/`finalize`/`RemoteFetch`) keeping `crates/fdae` async- and proxy-free; the receiving-side native `resolve-relation` method (A1 capability-gated / A2 `resolvable_without_capability`) in [synsvc_native.rs](file:///Users/pari/gitSyneroym/syneroym/crates/control_plane/src/synsvc_native.rs); the calling-side `resolve_fetches` orchestration seam over a real Universal Proxy, `expected_asserter_did` policy-declared trust anchor (D-B3-8), and successful-fetch `DecisionTrace` provenance in [fdae_fetch.rs](file:///Users/pari/gitSyneroym/syneroym/crates/rpc/src/fdae_fetch.rs); reference scenario step 23 (the federated fetch itself) and the timeout/asserter-mismatch deny-closed case, both proven across two genuinely independent real `syneroym-substrate` instances (distinct node identities, a real Iroh QUIC hop) in [federated_fdae_e2e.rs](file:///Users/pari/gitSyneroym/syneroym/crates/substrate/tests/federated_fdae_e2e.rs). `mise run test:e2e` (12/12 passing) is a regression gate on this work, not step-22 evidence: the harness deploys only a TCP passthrough service ([global-setup.ts](file:///Users/pari/gitSyneroym/syneroym/crates/substrate/tests/e2e/global-setup.ts), `svc deploy --tcp`) with no WASM component and no FDAE policy, so it exercises zero FDAE code. **Delivered (B3.5-fdae, 2026-07-25):** D-04-02-h closed for both guest-originated-read ingresses — the router-verified caller now threads through `dispatch.rs`'s `JsonRpcToWasm` branch into `HostState.caller` (ingress i, the WASM host-function path) in [engine.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/src/engine.rs), and `proxy::Host::call`'s self-proxy branch now forwards that same caller instead of re-synthesizing `service_system` (ingress ii, guest self-proxy into its own service's native `data-layer`) in [host_capabilities.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/src/host_capabilities.rs) — reference scenario step 22's "…never reaches the WASM guest" half is now shown correctly filtered for a real caller, proven in [data_layer_integration.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/tests/data_layer_integration.rs) and [proxy_dispatch.rs](file:///Users/pari/gitSyneroym/syneroym/crates/router/tests/proxy_dispatch.rs). **Delivered (B4-fdae, 2026-07-25):** the guest-exported stage-4 after-step (ADR-0017 §7) — policy opt-in per permission (`Permission.authorize_rows`) and `CompiledSieve.abac_permissions` in [crates/fdae](file:///Users/pari/gitSyneroym/syneroym/crates/fdae); the `RowAuthorizer` trait, DTOs, and the fail-closed `apply_stage4` orchestration helper (arity guard, over-large-batch/timeout/unavailable-authorizer denials, dotted-redact-path denial) in [fdae_abac.rs](file:///Users/pari/gitSyneroym/syneroym/crates/rpc/src/fdae_abac.rs); the `syneroym:data-layer/authorizer` WIT export in [authorizer.wit](file:///Users/pari/gitSyneroym/syneroym/crates/wit_interfaces/wit/data-layer/authorizer.wit); the engine's real, throw-away-instance `RowAuthorizer` implementation, the read-only substrate identity (`AuthLevel::LocalReadOnly`/`CallerContext::service_abac`) and `HostState.read_only` hard-deny on every mutating/egress host function, and dedicated `abac_max_instructions`/`abac_epoch_timeout_secs` fuel/time budgets in [engine.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/src/engine.rs)/[host_capabilities.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/src/host_capabilities.rs)/[config.rs](file:///Users/pari/gitSyneroym/syneroym/crates/core/src/config.rs); both read ingresses wired (`get`/`query`/`check-access` in [host_capabilities.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/src/host_capabilities.rs) for the WASM host-function path, and in [synsvc_native.rs](file:///Users/pari/gitSyneroym/syneroym/crates/control_plane/src/synsvc_native.rs) for guest self-proxy/native dispatch), plus `aggregate`/`delete_many` deny-closed under a stage-4 policy in [sqlite.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/src/sqlite.rs) and a `resolve-relation` deny for a stage-4-opted definition (`definition_has_abac`) in [synsvc_native.rs](file:///Users/pari/gitSyneroym/syneroym/crates/control_plane/src/synsvc_native.rs); deploy-time `validate_stage4_export` rejecting a stage-4-opted policy whose component doesn't export the after-step (WASM) or that names a TCP/container service at all, with policy-row rollback on rejection, in [orchestration.rs](file:///Users/pari/gitSyneroym/syneroym/crates/control_plane/src/service/orchestration.rs); the `abac-test` WASM fixture and 8 real end-to-end tests in [abac_integration.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/tests/abac_integration.rs); the router-side ingress-(ii) self-proxy proof in [proxy_dispatch.rs](file:///Users/pari/gitSyneroym/syneroym/crates/router/tests/proxy_dispatch.rs); the `resolve-relation` stage-4 deny regression tests in [native_dispatch_identity.rs](file:///Users/pari/gitSyneroym/syneroym/crates/router/tests/native_dispatch_identity.rs) (dispatched through the identical `SynSvcNativeService::resolve_relation` path a real federated fetch reaches; a genuinely cross-node proof in [federated_fdae_e2e.rs](file:///Users/pari/gitSyneroym/syneroym/crates/substrate/tests/federated_fdae_e2e.rs) is not possible with that harness's current TCP-typed data-owning node, see `deferred-backlog.md`); Failure/Security matrix rows 7-9 all ✅; the `< 25 ms` Mode-B budget's after-step counterpart measured via `criterion` in [abac_bench.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/benches/abac_bench.rs) (34.9 µs instantiation floor at 0 rows, 1.048 ms at the 1000-row `MAX_ABAC_ROWS` cap — negligible against the pushdown-query budget). **Known gaps, not this row's blocker:** a recursive-CTE row-count blowup under a guest-writable relation (H5, `status.md`'s third-pass review) is open, lower-urgency, and bounded in practice by the watchdog; the federated-hop perf budget (< 50 ms p99) is not met on the measured sandboxed test environment, dominated by `IrohHop`'s per-call fresh-connection establishment cost rather than the fetch's own processing time (tracked in `deferred-backlog.md`, not a B3 correctness gap); `resolve-relation`'s stage-4 deny is definition-level, coarser than a compiled sieve's per-read `abac_permissions` (D-B4-3), and a post-hoc after-step page can come back shorter than `limit` while `next_cursor` is still `Some` (D-B4-5) — both tracked in `deferred-backlog.md`. **Delivered (B5-fdae, 2026-07-26):** the write-side integrity half — every single-row `put`/`patch`/`delete`/`batch_mutate` now authorized against the caller's compiled FDAE policy, in the same SQLite transaction as the mutation, in [sqlite.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/src/sqlite.rs) (`authorize_and_mutate`/`row_reachable`/`do_authorized_put`/`do_authorized_patch`/`do_authorized_delete`); D-04-02-f resolved via Postgres's `USING`/`WITH CHECK` split (ADR-0017 §2.1) rather than a policy-schema amendment — a create's post-image check needs no new permission kind; CLS extended to writes (D-B5-7, a masked field cannot be authored on create nor changed on update); `System`-caller writes deny closed under a policy (D-B5-3); `creator_id` attribution unified across both ingresses via `CallerContext::write_attribution` (D-B5-5) in [native.rs](file:///Users/pari/gitSyneroym/syneroym/crates/rpc/src/native.rs); an upsert can no longer reassign a row's `creator_id` (D-B5-6); both ingresses wired in [host_capabilities.rs](file:///Users/pari/gitSyneroym/syneroym/crates/sandbox_wasm/src/host_capabilities.rs) and [synsvc_native.rs](file:///Users/pari/gitSyneroym/syneroym/crates/control_plane/src/synsvc_native.rs); `DecisionTrace` gained `operation`/`row_id`/`write_phase` in [trace.rs](file:///Users/pari/gitSyneroym/syneroym/crates/fdae/src/trace.rs); Failure/Security matrix rows 10-12 all ✅; the per-mutation `EXISTS`-check write-latency cost measured via `criterion` in [fdae_bench.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/benches/fdae_bench.rs) (a single authorized `patch` ~40 µs vs. ~21 µs unauthorized, both negligible against the 25 ms p99 Mode-B budget). Every M04B slice is now complete; this row closes the milestone's `[FND-IAM]` (M4B) work. |
| `[LFC-MGT]` (App Supervisor) | Multi-substrate placement + substrate inventory, unattended deploy with retry, health monitoring, bounded remediation, alerting, and dependency-binding propagation. Design of record: [ADR-0020](file:///Users/pari/gitSyneroym/syneroym/docs/decisions/0020-stable-logical-service-identity.md) (stable master DID per member of a logical service) and [ADR-0021](file:///Users/pari/gitSyneroym/syneroym/docs/decisions/0021-binding-propagation-and-app-supervisor.md) (bindings pushed into service config; no live directory). Supersedes the *Interstitial: Live App-Context Registry* placeholder and carries both of its goals forward, including `expected_asserter_did` publication (M04B Slice B3's D-B3-8 residual). | M5A | Complete | `docs/planning/milestones/M05A-app-supervisor/task.md` (Slices P0, A0-A5e, A7 — all Complete as of 2026-08-04, the milestone's own exit criteria). The `ControllerAgreement` creation tool row below (Slice P0) cleared this row's gate as of 2026-07-30 — a supervisor can now hold an authenticated deploy capability on a substrate the operator has claimed. Slice A6 (durable outbox/DLQ delivery) is deliberately out of this milestone and tracked in [deferred-backlog.md](file:///Users/pari/gitSyneroym/syneroym/docs/planning/deferred-backlog.md) §8 *Node lifecycle & ops* with an explicit pickup trigger (M5 item 1 Complete); its absence does not block this row, per the milestone's own exit criteria. Full evidence: `docs/planning/milestones/M05A-app-supervisor/status.md`. |
| `[FND-IDT]` (stable service identity) | Each **member** of a logical service has a master DID (one per member, not one per service, so round-robin and sharding keep distinct principals to select over); each physical instance holds a key delegated from it via the existing `DelegationCertificate`, so relocation, restart, and crash recovery stop changing who a member *is* to FDAE, to stored row attribution, and to issued capabilities. Adds two pieces that do not exist today: ingress-side `scope` enforcement, and endpoint records published under a master DID via a delegation-signed second acceptance path in the registry. As of M05A Slice A7, the **app instance** carrying those members also has a master DID of its own, minted at `adopt` and stable across a supervisor handover — the natural subject for a future access grant, where a grant against each member DID would change on every membership change. | M5A | Complete | [ADR-0020](file:///Users/pari/gitSyneroym/syneroym/docs/decisions/0020-stable-logical-service-identity.md) (amended 2026-07-28 against the tree A0 and A1 actually produced); `docs/planning/milestones/M05A-app-supervisor/task.md` Slices A0-A1, `status.md`'s A0/A1 sections. **Delivered (A0):** `SCOPE_ROUTING`/`SCOPE_SERVICE_INSTANCE`/`TRANSPORT_SCOPES` and `DelegationCertificate::verify`'s now-required `accepted_scopes` argument in [delegation.rs](file:///Users/pari/gitSyneroym/syneroym/crates/identity/src/delegation.rs); the ingress scope check in [handshake.rs](file:///Users/pari/gitSyneroym/syneroym/crates/router/src/handshake.rs); the instance-certificate store on `EndpointRegistry`/`EndpointStorage` across all four implementors in [local_registry.rs](file:///Users/pari/gitSyneroym/syneroym/crates/core/src/local_registry.rs)/[storage.rs](file:///Users/pari/gitSyneroym/syneroym/crates/core/src/storage.rs)/[registry_store.rs](file:///Users/pari/gitSyneroym/syneroym/crates/data_db/src/registry_store.rs); the `orchestrator/resolve-instance-identity` WIT query, deploy-time four-step certificate verification, and undeploy cleanup in [orchestration.rs](file:///Users/pari/gitSyneroym/syneroym/crates/control_plane/src/service/orchestration.rs); `ProxyRouter`'s guest-origin arm presenting a service's own certified instance key in [proxy.rs](file:///Users/pari/gitSyneroym/syneroym/crates/router/src/proxy.rs); `roymctl identity certify-instance`, `svc deploy --master`, and `app deploy --mint-masters` (post-compile service-id substitution, journaled before substitution so the journal never records master-DID-bearing plans) in [apps/roymctl](file:///Users/pari/gitSyneroym/syneroym/apps/roymctl/src/commands); the heartbeat near-expiry warning and `svc list` expiry column (D-A0-6's attended-posture visibility half) in [runtime.rs](file:///Users/pari/gitSyneroym/syneroym/crates/substrate/src/runtime.rs); Failure/Security matrix rows 1, 2, 3, 14 (A0's testable half) all ✅; two genuinely independent real substrates proving a distinct instance key per node and a stable member master across reinstantiation in [instance_identity_e2e.rs](file:///Users/pari/gitSyneroym/syneroym/crates/substrate/tests/instance_identity_e2e.rs). **Delivered (A1):** endpoint records published under the member master DID — `SignedEndpointInfo::verify`'s second, master-keyed acceptance path and the `RecordTrust::Publishing`/`Reading` split in [dht_registry.rs](file:///Users/pari/gitSyneroym/syneroym/crates/core/src/dht_registry.rs); the substrate-side `EndpointPublisher` that builds and signs the record at deploy and on the heartbeat in [endpoint_publisher.rs](file:///Users/pari/gitSyneroym/syneroym/crates/core/src/endpoint_publisher.rs); registry-local revocation-at-admission and the `refresh_master_anchor` read-modify-write (member masters now get an anchor at all) in [registry.rs](file:///Users/pari/gitSyneroym/syneroym/crates/community_registry/src/registry.rs)/[dht_registry.rs](file:///Users/pari/gitSyneroym/syneroym/crates/core/src/dht_registry.rs); `roymctl`'s three deploy shapes and anchor-refresh call sites in [apps/roymctl](file:///Users/pari/gitSyneroym/syneroym/apps/roymctl/src/commands); Failure/Security matrix rows 2 (narrow half), 3 (both halves), 4 all ✅; the reference scenario's step 4 relocation, proven live across two genuinely independent real substrates sharing one community registry — a member master DID resolves to node B's address, relocates to node A with no operator republish action, and a hand-forged record is rejected `401` at the registry — in [master_endpoint_record_e2e.rs](file:///Users/pari/gitSyneroym/syneroym/crates/substrate/tests/master_endpoint_record_e2e.rs). **Delivered (A2-A5e):** host-side dependency resolution and `expected_asserter_did` publication (A2); multi-substrate placement, the substrate inventory, health monitoring, the resident reconcile loop, unattended certificate renewal, and scale-out with the member dimension threaded through every store table and WIT field (A3-A5e) — see `status.md`'s A2-A5e sections for full evidence. **Delivered (A7, 2026-08-04):** an app-instance master DID, distinct from any member's, minted or resolved at `adopt` (`app-<app-instance-id>` in the supervisor's own vault, `MasterVault::get_or_mint` now taking a `MasterKind` so the mint warning names the right noun); recorded on the instance row and readable through `status` even while the vault is locked (`instance-status.app-master-did: option<string>`); movable through `export-master`/`import-master` under that name with no signature change; `adopt`'s result now a record (`generation`, `app-master-did`, `vault-name`) so an operator learns the backup command the moment the key exists. Proven live across two independent real substrates — `submit`, `adopt`, `status`, `export-master`, a second `adopt` reporting the identical DID at a higher generation — in [app_instance_identity_e2e.rs](file:///Users/pari/gitSyneroym/syneroym/crates/substrate/tests/app_instance_identity_e2e.rs); a handover across two vaults (one supervisor exports, a second that never adopted this instance imports and adopts without minting a second identity) proven at unit scale. Full evidence: `docs/planning/milestones/M05A-app-supervisor/status.md`'s A7 section. This row is now Complete. |
| `[PLT-DAP-05]` | Data Pipeline Streams (`syneroym:data/stream`) — point-to-point QUIC streams, QUIC-native flow control | M4A spike / M5 | Deferred | Moved wholesale to M5, decided 2026-07-18 — no code exists, and validating the framing choice has no real signal without M5's actual DataFusion consumer. See M04A Decision Register A.4 and `docs/planning/milestones/M04A-proxy-and-auth-foundation/status.md`'s "Slice A3 — DEFERRED TO M5" section. |
| `[LFC-VER]` (protocol negotiation) | Handshake protocol-version negotiation | Deferred (with wRPC) | Deferred | Minimal fail-fast unsupported-protocol error kept in M4A; full negotiation revisited alongside wRPC — see M04A Decision Register A.7. |
| `[FND-IAM]` (`ControllerAgreement` creation tool) | Tool to create/sign a `ControllerAgreement` — **pulled forward to M5A as Slice P0 (2026-07-27), shipped 2026-07-30**: `roymctl substrate claim`, the `security` interface gated on `substrate/admin`, and `build_caller`'s unowned-substrate bootstrap grant removed (fail-closed). The bundled registry-trust-model ADR is **discharged**: M05A Slice A1 (complete 2026-07-28) is the "real consumer" M04A Slice B7 gated this on — [ADR-0020](file:///Users/pari/gitSyneroym/syneroym/docs/decisions/0020-stable-logical-service-identity.md) §6's `verify()` change, reached from the opposite direction B7's F9 option 2 sketched. Multiple-substrate-owners representation (F12) and Tier 1 for the five data native-capability interfaces (F3) remain in M5 | M5A (tool) / M5 (F12, F3) | **Complete (tool, 2026-07-30)** | Spun out of M04A Slice B7 (`docs/planning/milestones/M04A-proxy-and-auth-foundation/plans/B7.md` §6, task.md item list after B7b) — B7b's ownership/deploy gate was inert until this tool existed, since nothing in the tree could create a `ControllerAgreement` (F4/F12). Evidence: `docs/planning/milestones/M05A-app-supervisor/status.md`'s P0 section. |
