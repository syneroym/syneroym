# Deferred Backlog

**Status:** Living document. **Last consolidated:** 2026-07-24.

This is the single running list of everything we have consciously **postponed, shortcut, or scoped out** during implementation — pulled together from the places that previously tracked deferrals in isolation:

- `system-requirements-spec.md` → [Appendix: Later-Phase Additions](../system-requirements-spec.md#appendix-later-phase-additions)
- `meta-implementation-plan.md` → [Later-Phase Additions](./meta-implementation-plan.md#later-phase-additions)
- Architecture Decision Records under [`docs/decisions/`](../decisions/)
- Per-slice "Out of scope" / "Deferred, recorded not dropped" sections in [`docs/planning/milestones/`](./milestones/)
- The [traceability matrix](./traceability-matrix.md) deferral rows
- In-code `TODO` / `FIXME` markers

Those sections remain the **authoritative source of record** for each item's rationale — this doc is the index that lets you see the whole backlog at once and find the source. When an item here disagrees with its source doc, the source doc wins; fix this doc.

## How to maintain this doc

**This is a running doc.** Whenever, in the course of implementing something, we **drop a feature, take a shortcut, gate something coarsely as a stand-in, or decide "later,"** add it here — as a final sanity step of the change, the same way we clean up imports and confirm the workspace builds. See the check in [`AGENTS.md`](../../AGENTS.md#ai-agent-guidelines).

- **Adding:** put the item under the right theme, state what's deferred and *why*, name a target milestone/phase if one exists (else `TBD`), and link the source of record (ADR, plan doc, or `file.rs:line`).
- **Landing (un-deferring):** when a deferred item ships, strike it or move it to [§ Recently resolved](#recently-resolved) with the commit/PR, and remove the corresponding in-code `TODO` marker.
- **Code markers:** every open `TODO(...)`/`FIXME` that represents a real deferral should have a home in [§ Open in-code markers](#open-in-code-markers). Markers that get resolved must be deleted from the code *and* removed here.

Target notation: `M05`, `M07`, `M10+`, `Phase 6` = a sequenced milestone/phase; `TBD` = deferred with no committed target; `blocks-prod` = a gap that blocks a production/multi-tenant posture and should not be forgotten.

---

## 1. Protocol & transport

| Item | What's deferred / why | Target | Source of record |
|---|---|---|---|
| wRPC binary wire protocol | Native `wrpc://` / `http-wrpc://` / `binary-wrpc://` schemes are reserved but not implemented; JSON-RPC 2.0 is the actual wire everywhere. | M05+ | [ADR-0014](../decisions/0014-quic-stream-protocol-routing.md); [system-architecture.md](../system-architecture.md) §wRPC; `router/src/preamble.rs`, `router/src/lib.rs` |
| Protocol-version negotiation `[LFC-VER]` | Handshake protocol-version negotiation deferred together with the wRPC wire. | M05+ | [traceability-matrix.md](./traceability-matrix.md); [M04A A1 plan](./milestones/M04A-proxy-and-auth-foundation/plans/A1.md) |
| Data Pipeline Streams `[PLT-DAP-05]` (QUIC point-to-point) | Moved wholesale to M5; WASI async streams not yet stable across runtimes. | M05 | [traceability-matrix.md](./traceability-matrix.md); [ADR-0010](../decisions/0010-mqtt-broker-rumqttd.md) |
| `crates/router` integration tests are flaky under full-workspace parallel execution | Two distinct sightings, same underlying shape, different test files and symptoms — plausibly a shared-state or resource-contention issue under concurrent load, not yet root-caused: (1) `proxy_dispatch.rs`, found during M05A Slice P0's review, passes 8/8 with `--test-threads=1` but fails intermittently under the default multi-threaded runner (a WASM trap inside `cabi_realloc`, surfaced as an RPC `-32603`, not a socket/bind error); P0's only change to `proxy.rs` is comments, so pre-existing, unrelated to that slice. (2) `native_dispatch_identity.rs`, found during M05A Slice A4's post-review pass, failed 4/39 in one `cargo test --workspace` run (a `mainline::dht` "actor thread unexpectedly shutdown: SendError" panic) but passed 39/39 immediately after when re-run in isolation; that pass touches no code in `crates/router` or the mainline DHT client. | TBD | `crates/router/tests/proxy_dispatch.rs`; `crates/router/tests/native_dispatch_identity.rs` |
| **No client in the tree can use the substrate's E2E encryption layer** | The router implements `?enc=ecdh-p256` termination (`route_handler/encryption.rs`, `routing.rs`), but `RoutePreamble::binary_json_rpc` hardcodes `enc: None`, `SyneroymClient` never sets it on either the Iroh or HTTP path, and there is no client-side ECDH anywhere — the only `enc = Some` is the preamble parser and the only ECDH bench is the server handshake. So the layer meant to protect traffic through an untrusted relay is unreachable in practice. Building the client half (ephemeral P256 keypair, handshake exchange, AES-GCM framing) against the existing server half would unblock an encrypted RPC path and would also need an invocation-level `transport_encrypted` flag to refuse a plaintext call. Found while planning M05A Slice A5, not caused by any A5 sub-slice, and larger than a supervisor slice should carry. | post-M5 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §0.29, §17; `crates/router/src/route_handler/encryption.rs`; `crates/router/src/routing.rs`; `crates/sdk/src/lib.rs` (`SyneroymClient`) |

## 2. Data layer

| Item | What's deferred / why | Target | Source of record |
|---|---|---|---|
| DuckDB / OLAP engine | DuckDB via the SQLite-scanner extension in the `olap` profile explicitly deferred. | Future phase | [system-architecture.md](../system-architecture.md); [system-requirements-spec.md](../system-requirements-spec.md); [M03 task](./milestones/M03-sss/task.md) |
| Init-defined logical views (F1/D3) | The `AggregationPipeline` shipped (B4); init-defined logical views on top of it did not. | TBD | [ADR-0007](../decisions/0007-data-layer-wit-interface.md); [M04A B4 plan](./milestones/M04A-proxy-and-auth-foundation/plans/B4.md) |
| Full MongoDB operator compatibility | Explicitly out of scope — only the supported operator subset is implemented. | Out of scope | [ADR-0007](../decisions/0007-data-layer-wit-interface.md) |
| Regex support in query layer | Deferred. | TBD | [ADR-0007](../decisions/0007-data-layer-wit-interface.md) |
| Logical data-service routing / replication | Routing a logical data service across nodes deferred to M5; log-replication overlays to M7. | M05 / M07 | [traceability-matrix.md](./traceability-matrix.md); [M03 task](./milestones/M03-sss/task.md) |
| WASM `migrate()` snapshot/rollback safety net | Migration path exists without a full snapshot/rollback safety net. | M05 | [ADR-0007](../decisions/0007-data-layer-wit-interface.md); `sandbox_wasm/src/engine.rs` |
| H5: recursive-CTE row-count blowup, and the FDAE watchdog budget it's bounded by is per-check, not per-request | The recursive CTE's `UNION ALL` plus a non-unique `from_key`/`to_key` join column lets row count blow up combinatorially instead of deduplicating (branching factor `b`, depth-64 bound → up to `b^64` rows) — confirmed structurally, no fix attempted (the obvious `UNION` swap doesn't work: rows carry `depth`/`seen`, rarely byte-identical; a real fix needs dedup on visited `id`, not path). Bounded today by `FDAE_MAX_VM_OPS`, but that budget is installed and dropped fresh per `EXISTS` check (`install_watchdog` in `row_reachable`/`do_check_access`/etc.), not once per request — a `batch_mutate` at `MAX_BATCH_SIZE = 200` runs up to 400 sieve evaluations in one transaction, each entitled to a full budget. On the write path this is worse than the read side's already-open exposure: reads spend the budget on a reader-pool connection, but writes spend it on **the single writer connection, holding a transaction open**, so an H5-shaped worst case there exhausts the one connection every write for the service needs, not one of many reader slots. Deliberately not fixed in the same pass that added the write-side gate: making the budget cumulative across a batch needs a shared counter threaded through every `row_reachable`/`do_check_access` call in a transaction (the progress-handler callback would need to decrement a per-request remaining-budget rather than always returning `true` on first fire), which is a real design/perf tradeoff of its own, not a drive-by fix. | TBD | [M04B task.md](./milestones/M04B-fdae-policy/task.md) matrix row H5; `crates/data_db/src/sqlite.rs` (`install_watchdog`, `row_reachable`, `do_check_access`) |

## 3. Access control, identity & security

| Item | What's deferred / why | Target | Source of record |
|---|---|---|---|
| Per-instance KEK "Model B" (IAM-gated provisioning) | Per-SynApp-instance/per-service KEK provisioning deferred, **not dropped**. Blocks multi-tenant production. | blocks-prod | [ADR-0006](../decisions/0006-sqlite-encryption-sqlcipher.md); [traceability-matrix.md](./traceability-matrix.md); [M04A B6 plan](./milestones/M04A-proxy-and-auth-foundation/plans/B6.md) |
| DB backup/restore has a raw-DEK chicken-and-egg gap | The wrapped DEK (`dek_store` table, AES-GCM-wrapped by an instance-KEK derived from the master KEK) lives *inside* the SQLCipher-encrypted DB file itself. A cold restore of a DB backup on another substrate can't open the file to read the wrapped DEK without the raw DEK first — no design yet for how backup/restore tooling caches or ships the raw DEK alongside a backup. No M7 milestone dir exists yet to hold this; tracked here until one does. | M7 `[FND-SEC]` Encrypted Backups (see [meta-implementation-plan.md:545](./meta-implementation-plan.md#L545)) | [ADR-0006](../decisions/0006-sqlite-encryption-sqlcipher.md); `crates/data_keystore/src/key_store.rs` (`generate_dek`/`load_dek`); `crates/data_db/src/sqlite.rs` (`resolve_dek`, `dek_store` schema) |
| Stage-4 ABAC widens `aggregate`/`delete_many`/single-row-write denial | `aggregate`/`delete_many` deny closed outright under a stage-4-opted policy (rows never surface to filter after the fact) — a new, real, user-visible restriction (a permission that used to work under CLS-only masking now hard-denies once `authorize_rows: true` is added), not merely a defensible extension of the existing CLS precedent at `sqlite.rs:636`. Slice B5-fdae extended the same deny-closed rule to single-row `put`/`patch`/`delete`/`batch_mutate` (D-B5-4): a mutation has no candidate-row batch to hand the after-step either, for the same reason. No mechanism exists to run the after-step over an aggregation pipeline's grouped/computed output, over rows about to be bulk-deleted before they're gone, or over a row mid-mutation. | TBD | [M04B slice-b4 plan](./milestones/M04B-fdae-policy/slice-b4-implementation-plan.md) §3.7/D-B4-3; [M04B slice-b5 plan](./milestones/M04B-fdae-policy/slice-b5-implementation-plan.md) §1 D-B5-4; `crates/data_db/src/sqlite.rs` (`do_aggregate`, `do_delete_many`, `authorize_and_mutate`) |
| `System`-caller writes deny closed under a policy | M04B Slice B5-fdae's write-side gate makes any policy-covered collection unwritable from every `AuthLevel::System`-caller ingress (`CallerContext::service_system`'s empty-capability session can never satisfy any permission) — the guest-mediated write paths only (`prepare_wasm_execution`'s anonymous-connection fallback, a cross-service proxied WASM call with `caller: None`, broker-delivered messages, `open_stream_instance`); the native ingress already refuses an anonymous caller outright, so it's unaffected. Deliberate (D-B5-3): exempting `System` would reopen the ingress-(ii) self-proxy bypass shape B3.5-fdae closed, and asymmetrically (a policy would apply to an anonymous visitor's reads but not their writes). Mitigation today: a collection a guest writes to as itself simply gets no `definitions:` entry, unless `strict: true`. Follow-up: thread a real principal into the anonymous-connection and proxied-WASM ingresses, the direction B3.5-fdae already set. | M04B follow-on | [M04B slice-b5 plan](./milestones/M04B-fdae-policy/slice-b5-implementation-plan.md) §1 D-B5-3; `crates/rpc/src/native.rs` (`CallerContext::service_system`); `crates/data_db/src/sqlite.rs` (`authorize_and_mutate`) |
| `check-access` cannot answer a create pre-check | Mode A's `check-access` answers about an *existing* row: for an id that does not exist yet it returns `false` (falls back to an existence probe when no policy is present), while Slice B5-fdae's `put` will *allow* the create if the post-image is reachable. A guest that pre-checks before creating would see "no" and then succeed anyway. Documented on the WIT (`data-layer.wit`) and `traits.rs` rather than fixed: making `check-access` answer a hypothetical post-image needs the payload it does not take today, a WIT signature change with no current consumer. | TBD | [M04B slice-b5 plan](./milestones/M04B-fdae-policy/slice-b5-implementation-plan.md) §3.6; `crates/wit_interfaces/wit/data-layer/data-layer.wit` |
| `resolve-relation`'s stage-4 deny is definition-level, coarser than a compiled sieve | Both A1 and A2 have no compiled sieve in hand for a structural resolution (A1's own `store.query` lets `data_db` compile internally; A2 has no sieve at all), so the stage-4 deny checks `definition_has_abac` (does *any* permission on the definition opt in) rather than a per-read `abac_permissions` (which permissions *this caller's* compiled sieve actually selected) — deliberately fail-closed, but coarser than necessary: a definition with one stage-4-opted permission and one that isn't denies `resolve-relation` for both. | TBD | [M04B slice-b4 plan](./milestones/M04B-fdae-policy/slice-b4-implementation-plan.md) §3.7/D-B4-3; `crates/control_plane/src/synsvc_native.rs` (`resolve_relation`); `crates/fdae/src/compile.rs` (`definition_has_abac`) |
| Stage-4 after-step can shorten a `query` page below `limit` while `next_cursor` is still `Some` | Post-hoc row removal by the after-step is new behavior the sieve alone never produced: previously a full page (`limit` rows) meant a full page. A caller paging on "results.len() < limit ⇒ done" instead of "`next_cursor` is `None` ⇒ done" now under-reads silently. Documented on the WIT `query` doc comment and in `traits.rs`; not backfilled (backfilling would mean an unbounded number of after-step invocations per call). | TBD | [M04B slice-b4 plan](./milestones/M04B-fdae-policy/slice-b4-implementation-plan.md) §1 D-B4-5; `crates/wit_interfaces/wit/data-layer/data-layer.wit`; `crates/data_db/src/traits.rs` |
| No genuinely cross-node proof of `resolve-relation`'s stage-4 deny | `federated_fdae_e2e.rs`'s two-real-substrate harness always deploys its data-owning node (`hr_service`) as a **TCP** service (`deploy_manifest`'s hardcoded `ServiceType::Tcp`) — and `validate_stage4_export` (Phase 4) correctly rejects any `authorize_rows: true` policy on a TCP service outright (no guest to call), so a stage-4-opted policy can never even be deployed on that node. Attempted during Slice B4-fdae, reverted when the deploy itself failed exactly as the gate is supposed to fail it. The local proof (`router/tests/native_dispatch_identity.rs::resolve_relation_a1_denies_closed_under_a_stage4_definition`/`..._a2_...`) dispatches through the identical `SynSvcNativeService::resolve_relation` path a real federated fetch reaches, so the deny logic itself is covered — what's missing is proof over a real Iroh QUIC hop specifically. Closing this needs a WASM-typed, `authorize-rows`-exporting data-owning node added to the harness (e.g. deploying `abac-test` or a similar fixture as Node A), a larger change to that 672-line fixture than justified by one regression assertion. | TBD | [M04B slice-b4 plan](./milestones/M04B-fdae-policy/slice-b4-implementation-plan.md) §4 "Regression surface for the resolve-relation deny"; `crates/substrate/tests/federated_fdae_e2e.rs`; `crates/control_plane/src/service/orchestration.rs` (`deploy_tcp_service`) |
| `resolve_fetches` fetch parallelism | Multiple distinct remote relationship fetches in one query resolve sequentially, not concurrently; correctness over scale (D-B3-6's own precedent), not a correctness dependency. | TBD (perf) | `crates/rpc/src/fdae_fetch.rs`; [M04B slice-b3 plan](./milestones/M04B-fdae-policy/slice-b3-implementation-plan.md) §5/D-B3-6 |
| Federated fetch has neither result caching (D-B3-6) nor connection reuse | Two compounding gaps in the same code path, merged into one item since fixing only one leaves the other still dominating: (1) no caching of a fetched `RelationshipProof` across calls — every `resolve_fetches` call re-fetches from the remote node, even for the same `(service, relation, principal)` across separate requests (ADR-0017 §6 calls batching + TTL'd caching "load-bearing … or Mode B does not scale" — shipped without it deliberately, correctness first, with the signed/TTL'd `RelationshipProof` record shape already in place so a cache is a pure additive follow-up); (2) `IrohHop` (`crates/router/src/proxy.rs`) opens a brand-new Iroh QUIC connection per call with no reuse — discovered measuring Phase 5's federated-fetch perf budget, connection establishment alone consistently dominated the measured latency (~4.5s observed vs. the < 50 ms p99 aspirational floor, zero retries logged, i.e. inherent to a fresh connection, not a symptom of anything else). Caching alone wouldn't help without connection reuse (a cache miss still pays the ~4.5s), and connection reuse alone still re-fetches identical data every request — together they mean the federated fetch is correctness-proven (Phase 5) but not close to production-ready at any real QPS. | blocks-prod | `crates/rpc/src/fdae_fetch.rs`; `crates/rpc/src/relationship_proof.rs`; `crates/router/src/proxy.rs`; [M04B slice-b3 plan](./milestones/M04B-fdae-policy/slice-b3-implementation-plan.md) §3.2/§5/§7/§9 D-B3-6; [ADR-0017](../decisions/0017-fdae-policy-schema-and-compilation.md) §6; `crates/substrate/tests/federated_fdae_e2e.rs` |
| Stage-4 `authorize_rows` opt-in is per permission, but not per row | A definition's applicable permissions OR into one compiled `where_clause`; if *any* applicable permission opts in, every row the combined clause admits is judged by the after-step — including a row a sibling permission that left `authorize_rows: false` alone would have admitted. Restrict-only, so not a leak, but `AbacAuthContext.permissions`/the WIT `auth-context.permissions` is batch-scoped, giving the guest no way to recover which permission actually reached a given row and apply different logic per rule. Post-review (2026-07-26, B4 review finding B4-05): documented explicitly on `Permission.authorize_rows` and the WIT `auth-context.permissions` field rather than implemented — carrying the admitting permission per row needs each OR-branch to retain its own provenance through `compile_read`, the same shape D-04-02-g's caveat-union problem already needs and doesn't have yet; fixing both together is more coherent than a one-off for stage-4 alone. | TBD | [ADR-0017](../decisions/0017-fdae-policy-schema-and-compilation.md) §7; `crates/fdae/src/policy.rs` (`Permission::authorize_rows`); `crates/wit_interfaces/wit/data-layer/authorizer.wit`; [M04B slice-b4 plan](./milestones/M04B-fdae-policy/slice-b4-implementation-plan.md) §5 item 6 |
| `abac_integration.rs`'s CI-hardened skip macro isn't the repo's default | Post-review (2026-07-26, B4 review finding B4-10): every other WASM-fixture-dependent test file (`data_layer_integration.rs`, `messaging_integration.rs`, `stream_integration.rs`, `proxy_dispatch.rs`, `lifecycle_hooks.rs`) still silently `eprintln!`s and returns when its artifact is missing, with no `CI`-gated panic. `abac_integration.rs`'s eight tests are Slice B4-fdae's Failure/Security matrix evidence, which justified the narrower fix there; the same silent-skip gap in the other files (including at least one, `data_layer_integration.rs`'s D-04-02-h tests, that is also security evidence) was left as the pre-existing house pattern rather than changed repo-wide in the same pass. | TBD | `crates/sandbox_wasm/tests/abac_integration.rs` (`skip_if_missing_artifact!`); `.github/actions/ci-build-and-test/action.yml` |
| Fine-grained caller authorization on routes | Current gate only proves *an* identity is present, not *which* callers may reach a route; coarse fail-closed stand-in in the proxy. | M04B follow-on | `router/src/route_handler/io.rs`, `router/src/proxy.rs` |
| `create-collection` stays ungated (unlike `drop-collection`, `execute-ddl`, `query-raw`) | Deliberate asymmetry, not an oversight: a never-yet-existing collection has no policy-protected rows to destroy (unlike `drop-collection`, which erases every row a write-capable-but-otherwise-unreachable caller couldn't delete individually), and `router/src/route_handler/io.rs`'s owner-rooting predicate (`is_root`) deliberately excludes any capability entailing `data-layer/admin` from per-service owner-rooted UCAN chains -- only the node's real admin root can ever hold it. Gating `create-collection` the same way would make it impossible for a service on an **unowned** substrate (M04A B7a F4's bootstrap posture, no `admin_ucan_root`) to provision its own schema at all, breaking self-service deploy. Revisit once `owner_of`-scoped admin-entailing grants exist. | TBD | `crates/sandbox_wasm/src/host_capabilities.rs` (`create_collection`); `crates/control_plane/src/synsvc_native.rs` (`"create-collection"` arm) |
| Policy language has no way to express "write-only, no read entailment" | `Ability::entails` makes `write` ⊇ `read` (the tiered `data-layer` hierarchy), so a permission like `allows: [data-layer/write]` with `paths: []` -- meant to admit an append-only/creation shape -- is applicable to a *read* too, for any caller holding a capability at `write` or above on that resource: `applicable_permissions` filters on the permission's own `allows` entailing the operation, not on the caller's ability, so there's no way for a policy author to declare "this permission's `paths` may authorize a write but must never be consulted for a read." Whether such a permission ends up leaking read access depends entirely on which capabilities the policy's other permissions and the caller's own grants put in play (post-review, 2026-07-26, B5-fdae review finding B5-01) -- not a bug, since the compiler's entailment behavior is correct and documented, but a real expressiveness gap: an author who wants true append-only-with-no-read semantics has no schema construct for it today. Fixing it needs either exact-ability matching for a permission's `allows` (opt out of entailment) or an explicit `write_only` marker -- a policy-schema change, not a compiler fix. | TBD | [M04B slice-b5 plan](./milestones/M04B-fdae-policy/slice-b5-implementation-plan.md) §1 D-B5-2; `crates/fdae/src/compile.rs` (`applicable_permissions`, `Ability::entails`) |
| `anchor_did` path-list binding | No near-term consumer; recorded and deferred beyond B3. | TBD | [access-control-design.md](./access-control-design.md); [ADR-0015 §A8](../decisions/0015-ucan-capability-model.md) |
| Pairwise DIDs | Costs FDAE dearly with no M4 consumer. | TBD | [access-control-design.md](./access-control-design.md); [ADR-0015 §A8](../decisions/0015-ucan-capability-model.md) |
| Stale-relationship-data handling; SCP-style node ceilings | Explicitly deferred ("do not build" for node ceilings). | TBD | [access-control-design.md](./access-control-design.md) |
| Multiple substrate owners (F12) | Multi-owner representation spun out of M04A B7 (Q5/F12). Not needed for single-owner multi-substrate placement, so it stays in M5 while the `ControllerAgreement` tool it was bundled with moves out (row below). | M05 | [M04A B7 plan](./milestones/M04A-proxy-and-auth-foundation/plans/B7.md); [traceability-matrix.md](./traceability-matrix.md) |
| **Stable member identity: unattended certificate renewal (online-key posture)** | **Not deferred past this milestone — it is A5 scope, written into A5's slice text.** [M05A slice A0](./milestones/M05A-app-supervisor/slice-a0-implementation-plan.md) D-A0-6 plans the **attended posture only**: an operator runs `roymctl identity certify-instance` on their own cadence, with a near-expiry `warn!` on the existing heartbeat sweep and an expiry column on `svc list` to make a lapse visible before it bites. Automatic renewal needs a component that holds member master keys and runs unattended — a substrate-side renewal timer would put the master key on the substrate, which [ADR-0020](../decisions/0020-stable-logical-service-identity.md) §3 forbids outright. So through A0-A4 a missed cadence is an **outage**, not a degradation (task.md failure matrix row 3), and `RotationPolicy` (which ADR-0020 §5 nominates for in-place-vs-restart replacement) stays unused because there is nothing yet for it to choose between. **A4 adds a third piece of evidence to the attended posture's visibility half**: `sdk::health::record_report` raises a `CertificateNearExpiry` alert using the same relative (25%-of-lifetime) rule the substrate's own heartbeat sweep uses (`DelegationCertificate::is_near_expiry`, moved to `syneroym-identity` so both sites share one definition). The row stays open — visibility is not renewal. | **M05A (A5)** | [ADR-0020](../decisions/0020-stable-logical-service-identity.md) §3/§5; [M05A slice A0 plan](./milestones/M05A-app-supervisor/slice-a0-implementation-plan.md) §1 D-A0-6; [M05A slice A4 plan](./milestones/M05A-app-supervisor/slice-a4-implementation-plan.md) D-A4-16 |
| **No detection of a pre-A0 service adopting a member master** | Redeploying a service that already holds data *with* a member master gives it a new authorization identity and orphans it from every row it wrote ([ADR-0020](../decisions/0020-stable-logical-service-identity.md) Context problem 2) — stated in [M05A task.md](./milestones/M05A-app-supervisor/task.md)'s migration section and accepted pre-release. The CLI cannot warn about it: the substrate has no logical-ref → service-id index to notice that this logical service was previously deployed under a different DID. A0 ships the mint-time backup warning and the migration note instead of detection. Genuinely optional — nothing later in M05A or M5 depends on it — but it becomes nearly free once A2 builds that index. **Pickup trigger: A2's logical-ref index lands** — A2's `service_app_context` table is exactly that index, keyed by `service_id`, not by logical ref; still TBD whether that shape is sufficient or a dedicated index is needed. | TBD (trigger-driven) | [M05A slice A0 plan](./milestones/M05A-app-supervisor/slice-a0-implementation-plan.md) §1 D-A0-9; [M05A task.md](./milestones/M05A-app-supervisor/task.md) migration impact |
| ~~**Cross-app `Bind` dependency naming has no manifest surface**~~ **Moved 2026-08-02** | No longer tracked here: it is committed platform work with a named owner and pickup trigger, not droppable debt. See [meta-implementation-plan.md § Committed Work: Logical Service Discovery Overlay](./meta-implementation-plan.md#committed-work-logical-service-discovery-overlay-2026-08-02) slice **S4**. [ADR-0022](../decisions/0022-two-tier-logical-service-discovery.md) §5 supplied the missing half — how a cross-app binding is authorized. | *(moved)* | [ADR-0022](../decisions/0022-two-tier-logical-service-discovery.md) §5; [M05A slice A2 plan](./milestones/M05A-app-supervisor/slice-a2-implementation-plan.md) §0.2, D-A2-2 |
| **`Relation.service` in an FDAE policy is not a declared-dependency name** | D-B3-8's publication gap is closed on the *credential* side (A2 declares `expected_asserter_did` as the member master and teaches `RelationshipProof::verify` to accept an instance-key signature carrying a delegation from it), but a policy's `expected_asserter_did` is still a hand-declared literal DID, not resolved through the same binding registry a guest's `dependency(...)` target uses. Making it derivable needs `syneroym_fdae::plan_read` to take a resolver — an API change reaching through `data_db`'s whole auth path and both read ingresses. Out of scope for A2 deliberately: the credential-survival half is load-bearing, the policy-derivation half is not. **Retargeted off A5**: a policy-authoring concern nothing in A5a-A5e touches — the supervisor's binding push changes *where a dependency resolves*, not how a policy declares its own `expected_asserter_did`. | TBD | [M05A slice A2 plan](./milestones/M05A-app-supervisor/slice-a2-implementation-plan.md) §0.3; [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §17; `crates/fdae/src/policy.rs` (`Relation.expected_asserter_did`) |
| ~~**`TopologyMode::Sharded` has no expressible sharding strategy in a manifest**~~ **Moved 2026-08-02** | No longer tracked here, for the same reason as the row above. See [meta-implementation-plan.md § Committed Work: Logical Service Discovery Overlay](./meta-implementation-plan.md#committed-work-logical-service-discovery-overlay-2026-08-02) slice **S1**. [ADR-0022](../decisions/0022-two-tier-logical-service-discovery.md) is why it acquired a target: `Sharded` needs a manifest strategy, an externally discoverable member set, a routing key expressible over HTTP, and an epoch fence — and is unusable with any one of the four missing, so they are sequenced together rather than left individually `TBD`. | *(moved)* | [ADR-0022](../decisions/0022-two-tier-logical-service-discovery.md); [M05A slice A2 plan](./milestones/M05A-app-supervisor/slice-a2-implementation-plan.md) §0.5, D-A2-4 |
| **A `RelationshipProof`'s delegation is not revocation-checked** | `HandshakeVerifier` consults the master anchor on an inbound *connection*, but a `RelationshipProof` arrives in a response **body**, which no handshake covers — so a revoked instance key can still produce an accepted proof until its certificate expires (bounded by the attended posture's certificate lifetime, 24h default). Fixing it means giving `RelationshipProof::verify` a `MasterAnchorResolver`, which `crates/rpc` cannot reach today without a dependency inversion. | TBD | [M05A slice A2 plan](./milestones/M05A-app-supervisor/slice-a2-implementation-plan.md) §5.1; `crates/rpc/src/relationship_proof.rs` (`verify`) |
| **Stale `StaticInventory` entries after undeploy or a dependency-dropping redeploy** | `undeploy`, and `install_app_context`'s call from a redeploy whose manifest drops a previously-declared dependency, both remove the service's persisted app-context and binding rows (`EndpointStorage`) but deliberately leave the in-memory `StaticInventory` entry alone (D-A2-9): a `TopologyEntry` is an app-scoped fact ("where does `backend` live in instance X"), not a per-dependent one, and removing it when one of several dependents goes away would break the others still relying on it — the same reasoning applies whether that dependent disappears via undeploy or via a redeploy that stops declaring the dependency (post-review: previously decided only for undeploy; extended explicitly to the redeploy case, code comment updated to match). A restart re-derives `StaticInventory` from `service_bindings` alone, so an entry no longer backed by any row silently drops out then, even though it kept resolving right up to that point — expected, not a bug. Bounded by (app instances × dependency names) and left for A5's lifecycle management to actually evict. **Retargeted off A5**: A5a's own eviction is scoped to `write-bindings`' membership-change path (ADR-0021 §3), not the undeploy/redeploy-drop case this row names — still nothing evicting `StaticInventory` on those paths. | TBD | [M05A slice A2 plan](./milestones/M05A-app-supervisor/slice-a2-implementation-plan.md) §3.4, D-A2-9; [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §17; `crates/app_orchestration/src/resolver.rs` (`StaticInventory`); `crates/control_plane/src/service/orchestration.rs` (`install_app_context`, `undeploy_impl`) |
| **A `write-bindings` epoch guard is per-dependent, but the resolver entry it writes into is app-scoped and shared** | `classify_binding_write`'s "held" state comes from the **per-dependent** `service_bindings` row (`binding_of(write.service_id, dependency_name)`), by design (D-A2-9's own doc: reading against the shared `StaticInventory` entry instead "would give every dependent on a node the same answer and produce false conflicts"). But the *apply* step still calls `logical_resolver.register(app_instance_id, dependency_name, entry)`, which writes the **one** shared `StaticInventory` entry every dependent naming that `(app_instance_id, dependency_name)` pair actually resolves through. Two dependents of the same instance therefore each track their own epoch for what is, physically, one target — and since only the pushed-to dependent's row and epoch advance, a *second*, independent push to the *other* dependent can present an epoch that is newer than that dependent's own (stale) row but **older** than what the first push already installed in the shared entry, and `classify_binding_write` will accept it as `Applied` and silently regress the entry everyone resolves through. Worse than a reporting mismatch: a supervisor relying on `status.binding-epochs` to judge convergence can be shown a stale-but-locally-consistent epoch while the actually-served target has already moved past it, or been regressed under it. Needs a design decision, not a drive-by fix: either the epoch guard must be evaluated against the shared entry for dependents that provably resolve to the same target (undoing part of D-A2-9's own reasoning), or `StaticInventory`'s key must grow `service_id` so each dependent genuinely owns an independent resolver slot (a bigger storage/resolver change). Flagged during A5a's review rather than fixed in it. | TBD | [M05A slice A2 plan](./milestones/M05A-app-supervisor/slice-a2-implementation-plan.md) D-A2-9; `crates/app_orchestration/src/resolver.rs` (`classify_binding_write`, `StaticInventory`); `crates/control_plane/src/service/orchestration.rs` (`write_bindings_impl`) |
| **A2's own two-substrate e2e coverage is partial** | A2's local (single-node) coverage is thorough: `router/tests/proxy_dispatch.rs`'s `guest_dependency_target_reaches_the_bound_member_and_a_re_registration_takes_effect_on_the_next_call` drives a real WASM guest through `syneroym:proxy/proxy::call` with `target-kind = "dependency"`, proving resolution and live re-registration end to end at the router level. What is **not** built: (1) a `dependency_binding_e2e.rs` two-real-substrate test (modelled on `master_endpoint_record_e2e.rs`) proving the reference scenario's step 4 from the *dependent's* side — a guest on node A reaching a member reinstantiated on node B with no binding rewrite; (2) `federated_fdae_e2e.rs`'s extension so the responding service holds an instance certificate and its `RelationshipProof` verifies against the member master (failure-matrix row 19, live, over a real hop) — today's Phase 3 coverage of that claim is unit-level only (`crates/rpc/src/relationship_proof.rs`, `crates/router/src/proxy.rs`). Both are meaningful two-real-substrate harness builds, judged out of proportion to add in the same pass as the rest of A2; the underlying mechanisms (delegation-carrying `RelationshipProof`, host-side resolution, cache eviction on write) are fully unit- and single-node-integration-tested. **A3 declined item (1) too, for the same reason**: its own plan sized a WASM-guest two-substrate dependency-call harness as the largest single item in the slice (it would have discharged this row and the `CallOrigin::Guest` row below together) and judged it out of proportion to add alongside A3's five other two-substrate e2e tests. Still open after three slices on the same missing harness. | TBD | [M05A slice A2 plan](./milestones/M05A-app-supervisor/slice-a2-implementation-plan.md) §7 "Integration / e2e"; [M05A slice A3 plan](./milestones/M05A-app-supervisor/slice-a3-implementation-plan.md) §9 test 6, §11; `crates/substrate/tests/master_endpoint_record_e2e.rs` (the harness shape to follow); `crates/substrate/tests/federated_fdae_e2e.rs` |
| **No live, wire-level proof that a guest-origin call presents its certified instance identity across a real cross-node hop** | A0's e2e extension landed as the scoped-down variant its own plan flagged as likely: [instance_identity_e2e.rs](../../crates/substrate/tests/instance_identity_e2e.rs) proves, over two genuinely independent real substrates, that `orchestrator/instance-identity` derives a distinct key per node, that `deploy` verifies and installs a certificate (rejecting a wrong one) live, and that the same member master authorizes a service reinstantiated on a second node. What it does **not** prove live is `ProxyRouter::invoke_remote_at`'s `CallOrigin::Guest` arm actually presenting that certified identity over a real cross-node QUIC hop — every real cross-node call `federated_fdae_e2e.rs`'s harness drives is `CallOrigin::Native` on a TCP service, and the guest arm only fires for a WASM guest's own outbound call. Building a WASM-guest two-node harness for that one wire hop was judged out of proportion to this slice; the guest arm's actual code path (preamble construction, certificate attachment) is proven at the router level instead (`crates/router/src/proxy.rs`'s `a_guest_call_travels_under_its_services_member_master_not_the_node_identity` and neighboring tests), just not across a live network hop. **A3 declined the same harness too** (see the A2 coverage row above, which names the same missing test) — not built again in this pass either, for the same proportionality reason. | TBD | [M05A slice A0 plan](./milestones/M05A-app-supervisor/slice-a0-implementation-plan.md) §5.4; [M05A slice A3 plan](./milestones/M05A-app-supervisor/slice-a3-implementation-plan.md) §9 test 6, §11; `crates/substrate/tests/instance_identity_e2e.rs`; `crates/router/src/proxy.rs` |
| **A5a's binding-push e2e test verifies the persisted row, not a live guest call** | Plan §8 test 36 asks for two things: a guest's next `dependency(...)` call resolving to the new member, and the frontend's wasm component never recompiling. `binding_push_e2e.rs`'s `a_membership_change_pushed_to_a_dependent_takes_effect_without_a_redeploy` asserts neither — it reads `status`'s `binding-epochs` and argues "without a redeploy" structurally (one `deploy` call, one `write-bindings` call, never a second `deploy`). The reduction is the same missing WASM-guest two-substrate harness the two rows above already track (no test-component in this tree exports a `dependency(...)` test driver), just hit again from A5a's own scope. The underlying mechanism — `write_bindings_impl` mutating `StaticInventory` and the resolver cache eviction on write — is unit-tested (`crates/app_orchestration/src/resolver.rs`, `orchestration.rs`'s `write_bindings_refuses_*` tests); what is not proven live is that a running guest's own next call actually observes it. | TBD | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §8 test 36; `crates/substrate/tests/binding_push_e2e.rs` |
| **`remove_instance_cert`/`remove_owner` are not atomic with their storage write** | Both delete from `storage` first and return early on `Err`, leaving the in-memory `DashMap` entry (and, for the cert, a later deploy's inherited-but-unusable credential) if the storage delete fails. Not fixed for A0-02's redeploy-clears-cert case alone: `remove_instance_cert` and `remove_owner` share the identical shape in `crates/core/src/local_registry.rs`, so patching only the newer cert path would leave the older, structurally identical owner path inconsistent. Needs a single symmetric fix (or a shared helper) covering both, not two asymmetric ones. `remove_owner`'s half of this has a live consequence as of M05A Slice P0: a failed removal blocks a *different* caller's later redeploy of that `service_id` ("ID squatting", `crates/router/tests/service_ownership.rs`'s `failed_remove_owner_blocks_a_different_callers_later_redeploy`), and that now bites an ordinary, non-node-wide caller on any substrate a `ControllerAgreement` has claimed, not only a hypothetical one. | TBD | `crates/core/src/local_registry.rs` (`remove_instance_cert`, `remove_owner`) |
| **A redundant supervisor holding one master via `import-master` on two live processes has no compare-and-set for master-anchor refresh** | A5d's scheduled anchor refresh and its revocation write are both read-modify-write against the registry's current anchor. Neither needs a compare-and-set under the topology this tree supports today: mint-in-place means exactly one `MasterVault` ever generates a given master, and `export-master`/`import-master` move a *file*, not concurrent live access — so there is structurally one writer, which A5d documents as the accepted invariant rather than building a CAS the registry does not expose. A redundant-supervisor deployment sharing one master across two live processes breaks that assumption and would need the registry (`crates/community_registry`) to grow real compare-and-set semantics, or an explicit single-writer lease. Pairs with A6's own single-writer cron-lease note — both are instances of the same "more than one supervisor for one thing" problem. **[M05B](./milestones/M05B-async-primitives/task.md) deliberately passes over this row rather than closing it** (2026-08-04): it builds no compare-and-set and no lease, because this row's own reasoning is why — the case is unreachable by construction under mint-in-place, not blocked on a missing primitive. Recorded here so the next reader does not read the omission as an oversight. | post-M5 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §26.8, D-A5d-8; `crates/core/src/dht_registry.rs` (`refresh_master_anchor`, `revoke_instance_key`) |
| **`renew-cert` by a node-wide grantee who is not the recorded owner re-keys the service to itself** | `renew_cert_impl` verifies the submitted certificate against `derive_service_identity(&caller.caller_did, &service_id)` -- the same rule `deploy` uses -- so a caller admitted only through the owner-or-node-wide check (not because it *is* `registry.owner_of`) can only pass a certificate for *its own* derived identity, and installing it moves the service's live derived key to that caller, silently. The previous owner's key is not revoked; it just stops being current, and stays valid until its own certificate's natural expiry. Pinned as today's deliberate behavior by `renew_cert_by_a_node_wide_deploy_grantee_ignores_the_service_owner` (`crates/control_plane/src/service/orchestration.rs`) and predicted by [ADR-0020](../decisions/0020-stable-logical-service-identity.md)'s 2026-07-28 amendment (after Slice A0), item 5: "`owner_did` is the deploying operator's DID... it will matter when the paired milestone's supervisor slice reasons about revoking a key it did not itself issue." A5d is that slice, and under unattended renewal this is no longer a rare manual act -- a supervisor that is node-wide-granted but not the recorded owner of an adopted-but-not-yet-redeployed member (the same placement shape a sibling A5d review finding names for `revoke-instance`) would re-key it silently every `renewed_cert_expires_hours`. An owner-preserving fix needs the renewing caller to learn the *owner's* prospective derived identity to mint against -- nothing exposes that today, and `instance_did` on `resolve-instance-identity` cannot be repurposed for it (the certify flow depends on it staying the caller's own). Documented on the WIT `renew-cert` doc comment in the meantime, not fixed. | TBD | [ADR-0020](../decisions/0020-stable-logical-service-identity.md) 2026-07-25 amendment item 5; [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §29; `crates/control_plane/src/service/orchestration.rs` (`renew_cert_impl`); `crates/control_plane/src/synsvc_native.rs` (`SynSvcNativeService::new`); `crates/wit_interfaces/wit/control-plane/control-plane.wit` (`renew-cert`) |
| **`revoke-instance` has no path back** | Once a placement gets a row in `revoked_placements`, nothing removes it: an operator who revoked a key by mistake, or who wants to bring a member back under management after replacing its instance key by hand, has no verb for it. The table gives an "un-revoke" a concrete place to write to, but A5d does not build the write. Needs decided semantics for what un-revoking means — in particular whether it must also remove the DID from the master anchor's `revoked_keys`, which is a separate and harder read-modify-write than clearing one local row, and arguably should never be undone at all. Not needed to close failure-matrix row 14b. | TBD | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §30, D-A5d-15; `crates/app_supervisor/src/store.rs` (`revoke_placement`) |
| **A relocated-away substrate keeps trying to publish a member's record, uselessly** | A relocation that leaves the old instance deployed no longer causes a permanent flap between two `substrate_id` values: every endpoint record now carries a monotonic pkarr/BEP44 timestamp, and both the DHT and the HTTP registry admit a record only if it is newer than what is stored (or an identical refresh), so the old substrate's replayed record is permanently rejected everywhere once the master signs a newer one for the new placement. What remains: the old substrate does not know it has lost and keeps trying every heartbeat, uselessly. Needs a publisher generation stamp or the supervisor's own placement view to actually stop it from trying. Narrowed from "the flap is permanent" to this by [ADR-0020](../decisions/0020-stable-logical-service-identity.md) §6's compare-and-swap admission rule. **A3 does not fix this** — it narrows the exposure by refusing the redeploy that would *create* a two-publisher state (D-A3-12: moving a service to a different substrate is a hard error, not silently applied), but does nothing for an old substrate that already kept running some other way. **Retargeted to post-M5**: needs a publisher generation stamp or the supervisor's own placement view, neither of which A5a-A5e build — the supervisor this milestone ships manages one substrate's own services, not cross-substrate placement awareness. | post-M5 | [M05A slice A1 plan](./milestones/M05A-app-supervisor/slice-a1-implementation-plan.md) D-A1-11, D-A1-14; [M05A slice A3 plan](./milestones/M05A-app-supervisor/slice-a3-implementation-plan.md) D-A3-12, §11; [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §17; `crates/community_registry/src/registry.rs` (`admit_endpoint`) |
| **A retired member's endpoint record lives until its TTL** | The HTTP registry has no unregister endpoint, so `undeploy` cannot remove a published record — it ages out on the registry's TTL clock instead. Pre-existing; more visible once relocation is real (A3/A5). Note the mechanism to fix this cheaply now exists (a superseding or tombstone record, keyed newer per D-A1-14) but is not wired to `undeploy` — `undeploy` only deletes the local stored file, never publishes anything. | TBD | [M05A slice A1 plan](./milestones/M05A-app-supervisor/slice-a1-implementation-plan.md) §7; `crates/control_plane/src/service/orchestration.rs` (`undeploy`) |
| **No way to renew an endpoint record's `not_after` without a full redeploy** | `EndpointInfo.not_after` defaults to 30 days (`DEFAULT_ENDPOINT_NOT_AFTER_SECS`) and is set once, at whatever `svc deploy --master`/`--identity` call produced the stored record. `renew-cert` installs a certificate and nothing else — the record is signed once at deploy and replayed verbatim by the substrate, which holds no master key to re-sign it with. The only way to extend it today is a full redeploy. **A5D-13 review, re-pointed**: this row used to name `roymctl identity certify-instance` (the attended-posture, connection-level renewal command) as the missing surface and call the gap "not urgent" on the strength of the 30-day default. Neither holds up post-A5d: `identity certify-instance` only ever touched the connection-level certificate, never the endpoint record, so it was never the right surface regardless of urgency — and A5d put the master, the renewal schedule, and the anchor-refresh precedent for "a periodic write against something the supervisor's own vault holds" all in one place (`crates/app_supervisor`), which makes the supervisor's resident loop the obvious home for this, not a `roymctl` verb. With `renewed_cert_expires_hours` now 4 hours by default under unattended renewal, the endpoint record's 30-day `not_after` is the real remaining bound on "deploy it and walk away" — everything else in the renewal story now runs on a much shorter clock than this one. | TBD | [ADR-0020](../decisions/0020-stable-logical-service-identity.md) §6; [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) Part V (§26-§32); `crates/core/src/dht_registry.rs` (`DEFAULT_ENDPOINT_NOT_AFTER_SECS`); `crates/core/src/endpoint_publisher.rs`; `crates/app_supervisor/src/service.rs` (`refresh_due_master_anchors`, the closest existing precedent) |
| **Operator-typed `--master`/`--identity`/`--name` values reach `<dir>/identities/<name>.key` with no path-traversal guard** | `resolve_member_master`, `member_master_path`, and the pre-existing `load_identity` all build the key path via `dir.join("identities").join(format!("{name}.key"))` with no rejection of `/` or `..`. Pre-existing (`load_identity` predates A0 with the identical shape) and low severity — the operator running `roymctl` already has OS-level access to whatever local file a crafted `--master ../../x` would reach, so this is not a privilege boundary, just unvalidated input. Not fixed piecemeal for A0's two new entry points (`resolve_member_master`, `svc deploy --master`) alone, since that would leave `load_identity`'s identical, older call site inconsistent — one shared validation helper across all three is the right fix. | TBD | `apps/roymctl/src/commands/member_identity.rs` (`resolve_member_master`, `member_master_path`); `apps/roymctl/src/commands/svc.rs` (`load_identity`) |
| Hardware attestation; supply-chain binary signing | Deferred to M7. | M07 | [M02 task](./milestones/M02-reliable-node/task.md) |
| Master key export/recovery; Tier-1 fallback; ZK-proof plugin `[FND-IDT]` | Deferred to the later-phase additions. | M10+ | [meta-implementation-plan.md](./meta-implementation-plan.md#later-phase-additions) |
| W3C Verifiable Credential compatibility | Delegation-cert format does not target VC data model yet. | TBD | [ADR-0001](../decisions/0001-delegation-certificate-format.md) |
| Auto-unseal (e.g. AWS KMS) | Out of scope for the substrate. | Out of scope | [ADR-0006](../decisions/0006-sqlite-encryption-sqlcipher.md) |
| **`Capability::grants` wildcards *any* bare `substrate:` resource, not just this node's own** | `is_substrate_scope` checks the `substrate:` prefix only, not which node's DID follows it, so `grants` treats a `substrate:<other-node>` + `substrate/admin` capability as covering everything, same as a same-node one. Defense-in-depth only, not a live hole: `build_caller` only ever issues capabilities on this node, and the UCAN root check applies `resource_is_local` separately. Unreachable today but worth closing before a capability naming another node's substrate could ever legitimately exist (e.g. cross-substrate delegation). | TBD | [M05A slice P0 plan](./milestones/M05A-app-supervisor/slice-p0-implementation-plan.md) §8; `crates/ucan/src/capability.rs` (`grants`, `is_substrate_scope`) |
| **`ControllerAgreement` ownership transfer / revocation has no story** | `roymctl substrate claim --force` replaces the agreement on the next restart, and that is the entire mechanism — no revocation, no multi-owner (F12, already tracked separately), no audit trail of who claimed a substrate when. | M05/TBD | [M05A slice P0 plan](./milestones/M05A-app-supervisor/slice-p0-implementation-plan.md) §8; `apps/roymctl/src/commands/substrate.rs` (`Claim`) |
| **App-instance lifecycle verbs need node-wide `orchestrator/deploy`** | A5a's `claim-app-instance`/`release-app-instance` are gated at the same node-wide `orchestrator/deploy` ability as `deploy` itself, because no narrower selector namespace exists for an app instance — `app/<name>` would put app-instance ids and service ids in one resource namespace, which is a bigger decision than A5a's own scope. A caller granted only enough to claim/release one app instance today must instead hold node-wide deploy. A narrower resource shape needs a selector-namespace decision (a new first segment, plus the `covers_resource` docs and tests that go with it). **A5b found the cost is actually wider than §0.28 stated**: a supervisor needs node-wide `orchestrator/status` too, not only `deploy` — `resolve-instance-identity` (certification, on the path of every deploy the supervisor performs) and `app-instance-management-of` (`adopt`'s read half, before any claim exists to match ownership against) both gate on `orchestrator/status` alone, and the two abilities are deliberately flat (neither entails the other). The supervisor e2e fixture (`crates/substrate/tests/supervisor_interface_e2e.rs`) issues both abilities together; the developer guide says so. | post-M5 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §0.28, §17; `crates/control_plane/src/service/orchestration.rs` (`claim_app_instance_impl`, `release_app_instance_impl`, `instance_identity`, `app_instance_management_of_impl`) |
| **`supervisor` read verbs (`status`/`alerts`) are gated at `substrate/admin`** | Every verb on the `supervisor` interface, including the two read-only ones, requires node-owner authority — a coarse stand-in (§11.2): `status`/`alerts` are monitoring reads and should eventually be holdable by a monitoring-only credential (a `supervisor/status` ability, mirroring `orchestrator/status`), so a read-only operator does not need full node-owner authority just to watch health and alerts. | post-M5 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §11.2, §17; `crates/app_supervisor/src/service.rs` (`require_admin`) |
| **Master backup requires collecting a file from the supervisor's host** | Mint-in-place (D-A5-27) means the operator holds no member master until `export-master` writes it into `master_backup_dir` on the supervisor's own host; retrieving it from there (a mounted volume, a backup agent, `scp`) is the operator's own arrangement — `task.md`'s non-goals already call backup "an operator duty this milestone documents rather than automates." What's new versus A0-A4's file-backed masters is that the file now starts on the supervisor's host rather than the operator's own. A remote `export-master`/`import-master` that took a caller-supplied destination would put the key on the wire, so this is gated on the client-side E2E encryption row above, not solvable independently. | post-M5 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §0.29, §0.31, §17; `crates/app_supervisor/src/keys.rs` (`MasterVault::export_master`/`import_master`) |
| **The supervisor's vault is locked until an operator injects the KEK, and the KEK does not survive a restart** | `KeyStore` is memory-only (`kek: Arc<Mutex<Option<Zeroizing<[u8; 32]>>>>`), its sole production populator is the `security.inject-kek` RPC, and no config field or environment variable supplies one. A rebooted supervisor therefore cannot mint, certify, or renew a member master until a human runs `inject-kek` again — bounding ADR-0020 §3's "issues and renews unattended" to *unattended between KEK injections*, not truly unattended. A5b surfaces this rather than fixing it: a loud startup warning (`init_supervisor`, `crates/substrate/src/runtime.rs`), a `submit`/`export-master`/`import-master`/`get_or_mint` refusal naming `inject-kek` (`VaultError::Locked`, `crates/app_supervisor/src/keys.rs`), and (once A5d's loop exists) a `VaultLocked` alert. A restart-surviving KEK is a key-management design with its own threat model, inherited from M04A, not introduced here. | post-M5 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §0.31, D-A5-28, §17; `crates/data_keystore/src/key_store.rs`; `crates/app_supervisor/src/keys.rs` |
| **`test_wit_adherence`-shaped tests catch a missing dispatch arm but never an extra one** | True of both `orchestrator`'s adherence test (`crates/control_plane/src/service.rs`) and the new `supervisor` one (`the_supervisor_wit_dispatch_table_covers_every_declared_function`, `crates/app_supervisor/src/service.rs`): both walk the WIT interface's declared functions and assert each dispatches, but neither walks the dispatch `match` the other direction, so an arm for a method no WIT interface declares is unreachable dead code nothing flags. | TBD | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §13, §17; `crates/control_plane/src/service.rs`; `crates/app_supervisor/src/service.rs` |
| **A supervisor holds no issued grants on the substrates it manages, and `submit` cannot bootstrap its own authority** | A4 §13's tension, still unclosed: `submit` refuses an inventory alias that carries no credential (§11.2) rather than obtaining one automatically. Issuing a node-wide `orchestrator/deploy` + `orchestrator/status` grant to a supervisor principal on each substrate it should manage is operator work with no tooling — the developer guide documents the manual `roymctl identity issue-grant` steps, and the e2e fixture hand-issues the same shape of token a real operator would need to produce and place into `inventory-json` themselves. | post-M5 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §11.2, §13, §17; `crates/app_supervisor/src/service.rs` (`build_clients`) |
| **A `roymctl substrate claim --expires-days` bound is checked only at boot** | `SubstrateIdentityState::init` evaluates `expiresAt` once, during `setup_substrate_identity`, and the result is flattened into a plain config string for the process's whole lifetime (`runtime.rs`). A controller whose agreement expired an hour ago keeps `substrate/admin` — deploy, undeploy, KEK injection and rotation — on a long-running node until someone restarts it. `--expires-days`'s help text now says so; nothing re-checks it at runtime. Same shape as the *"Claiming a substrate requires a restart"* row above (no reload path, unlike TLS's `SIGUSR1`), just the opposite direction (ceasing to hold, not starting to). | TBD | [M05A slice P0 plan](./milestones/M05A-app-supervisor/slice-p0-implementation-plan.md); `crates/identity/src/substrate.rs` (`SubstrateIdentityState::init`); `apps/roymctl/src/commands/substrate.rs` (`Claim`) |
| **A deploy-only grantee's rollback attempt is denied a second time, on a confusing error** | `deploy` calls `self.undeploy(service_id.clone(), caller)` with the *same* caller on two failure paths; abilities are deliberately flat and independently grantable, so "deploy but not undeploy" is a real, supported grant shape — but a deploy-only grantee (`roymctl identity issue-grant --can orchestrator/deploy`, no `orchestrator/undeploy`) whose deploy fails partway is denied *again* by `undeploy`'s own admission check on the rollback attempt, instead of getting a clean cleanup. Inert before `ControllerAgreement` existed (every verified caller held all three orchestrator abilities for free on an unowned substrate); M05A Slice P0 makes real app-scoped grants live, so the gap is now reachable. A grant meant to let its holder deploy reliably should include `orchestrator/undeploy` alongside `orchestrator/deploy`. **Retargeted from A3 to A5**: A3's substrate inventory *holds* a credential per substrate (`identity`/`ucan` per entry), it does not *issue* one — nothing in A3 mints grants. The developer guide's inventory walkthrough now names the three abilities an entry's credential needs together (`orchestrator/deploy` + `orchestrator/undeploy` + `orchestrator/status`) as operator guidance in the meantime. **Retargeted off A5**: a grant-issuance ergonomics gap in `roymctl identity issue-grant`, untouched by A5a-A5e's substrate-side write primitives or supervisor work. | TBD | `crates/control_plane/src/service/orchestration.rs:1255-1280` (the `undeploy` comment already documents the interaction in detail); [M05A slice A3 plan](./milestones/M05A-app-supervisor/slice-a3-implementation-plan.md) §0.4, §11; [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §17 |

## 4. Storage / blob

| Item | What's deferred / why | Target | Source of record |
|---|---|---|---|
| Public (unsigned) blob HTTP serving | Only signed-URL HMAC serving exists; public unsigned serving deferred. | TBD | [ADR-0009](../decisions/0009-blob-storage-object-store.md); `router/src/route_handler/http.rs` |
| `blob(list<u8>)` WIT arm | A possible future WIT extension, out of scope. | Out of scope | [M04A B5 plan](./milestones/M04A-proxy-and-auth-foundation/plans/B5.md) |

## 5. Messaging

| Item | What's deferred / why | Target | Source of record |
|---|---|---|---|
| MQTT shared subscriptions (`$share/<group>/<filter>`) | Small `namespace_topic` fix in `crates/mqtt_broker`; `rumqttd 0.20` already supports it. Doesn't block later milestones. | post-M3B, low pri | [meta-implementation-plan.md](./meta-implementation-plan.md#later-phase-additions); [M03B task](./milestones/M03B-messaging/task.md) |
| WebSocket upgrade for messaging | Out of scope for the messaging slice; revisit as follow-up. | TBD | [M03B task](./milestones/M03B-messaging/task.md) |
| Guest delivery p99 CI regression gate | `test_guest_delivery_latency_budget`'s p99 assertion (guarding the <25ms exit criterion, met at 5.16ms when first measured) downgraded to an informational log after two rounds of threshold-raising (75ms, 150ms) each got outrun by shared-CI-runner noise (observed 4ms-334ms with no dispatch-path change). A fixed millisecond bound in this integration test can't tell "CI was busy" from "the path regressed"; real regression detection needs a dedicated benchmark with proper statistics. | TBD | `crates/sandbox_wasm/tests/messaging_integration.rs`; [M03B status.md](./milestones/M03B-messaging/status.md) |
| `deliver_message` retry is bounded, not full redelivery | Root cause of the intermittent `test_guest_delivery_latency_budget` "timed out waiting for warm-up delivery" CI failures (previously masked by widening the watchdog): a transient failure instantiating the guest `Store` (pooling-allocator instance cap momentarily saturated, `build_wasm_engine`) or a trap during `handle-message` (e.g. an epoch-deadline hit while the runtime was starved) used to discard the message permanently on the first occurrence, no redelivery. Fixed with a bounded retry (4 attempts, 50ms backoff) in `deliver_message`. Reproduced and verified under synthetic CPU-contention stress (25-way oversubscription plus a concurrent full workspace test run): failure rate dropped from ~1-in-7 to ~1-in-25 runs, but did not reach zero under that level of artificial load -- a bounded retry can reduce, not eliminate, loss under sufficiently severe *sustained* (not just momentary) contention. A guest that traps on every attempt (genuinely broken component, not contention) is still, by design, dropped after 4 attempts rather than retried forever. Full at-least-once delivery (broker-level ack/redelivery, MQTT QoS-style) is out of scope of this fix. | TBD | `crates/sandbox_wasm/src/engine.rs:940` (`deliver_message`) |
| `mqtt_broker` subscriber channel drops on backpressure | `MqttBroker`'s per-subscription forwarding task `try_send`s into a bounded `mpsc` channel (default capacity 1024) and silently drops the message (`warn!`-logged only, no redelivery, no signal to the publisher) if the subscriber isn't draining fast enough. Not the cause of the `deliver_message` flakiness above (this test's volume never approaches the channel capacity), but the same class of unacknowledged at-most-once delivery, broker-wide rather than guest-dispatch-specific. | TBD | `crates/mqtt_broker/src/lib.rs:210` |
| Test fixture `handle_message`'s non-atomic next-id read-then-write | `messaging-pubsub-test`'s `handle-message` computes its next record id as `existing.records.len()` (a query) and then writes that id -- safe today only because each (service, topic) subscription has exactly one sequential forwarding task delivering to it one at a time. Two concurrent deliveries to the same service (e.g. two live subscriptions landing near-simultaneously) would race on the same id and one write would silently clobber the other. Latent, not yet triggered by any test. | TBD | `test-components/messaging-pubsub-test/src/lib.rs:126` |
| Decentralized messaging overlay | Pushed to M7. | M07 | [M03B task](./milestones/M03B-messaging/task.md); commit `c619b71` |
| Raw-timestamp plausibility bounds | Left open for a follow-up decision. | TBD | [ADR-0013](../decisions/0013-p2p-messaging-architecture.md) |

## 6. Payments & economics

| Item | What's deferred / why | Target | Source of record |
|---|---|---|---|
| Escrow / dispute-mediated custody | Not part of MVP payment surface; needs separate spec + legal review. | M11+ / Phase 6 | [system-requirements-spec.md](../system-requirements-spec.md#appendix-later-phase-additions); [m0-status](./milestones/m0-status.md) |
| System coins & mutual credit (DLN) | Native ledger token + bilateral IOU layered on the Payment Abstraction Layer; needs legal review + DLN scoping. | M11+ / Phase 6 | [system-requirements-spec.md](../system-requirements-spec.md#appendix-later-phase-additions); [meta-implementation-plan.md](./meta-implementation-plan.md#later-phase-additions) |

## 7. Gateway & networking

| Item | What's deferred / why | Target | Source of record |
|---|---|---|---|
| Client-gateway remote-access security | Currently local-machine-only as a stopgap; proper remote-access auth not designed. | TBD | `client_gateway/src/gateway.rs` |
| Gateway caller = substrate-owner DID threading | Present the controller DID as caller identity through the gateway. **New consequence since [M05A Slice P0](./milestones/M05A-app-supervisor/slice-p0-implementation-plan.md)** (2026-07-30): the gateway still presents the *node's* DID, which now holds nothing node-wide (an unowned/non-controller DID fails closed everywhere). Inert today only because the gateway proxies exclusively to deployed services, never to `orchestrator`/`security` — a routing accident, not a guarantee. | post-B0 | `client_gateway/src/gateway.rs` |
| Non-IP mesh transport (Zigbee / Thread / BLE) | Integrating non-IP mesh into the IP topology. | Later phase | [system-requirements-spec.md](../system-requirements-spec.md#appendix-later-phase-additions) |

## 8. Node lifecycle & ops

| Item | What's deferred / why | Target | Source of record |
|---|---|---|---|
| Structured retry policy on `SubstrateConfig` | Config does not yet expose a structured retry policy. | TBD | [ADR-0003](../decisions/0003-retry-policy-ownership.md); [M02 task](./milestones/M02-reliable-node/task.md) |
| Resource quotas on `ServiceManifest` | Manifest types have no resource-quota fields yet. | TBD | [M02 task](./milestones/M02-reliable-node/task.md); [ADR-0019](../decisions/0019-deploy-time-artifact-delivery.md) |
| WASM fuel metering | Not yet configured. | TBD | [ADR-0005](../decisions/0005-wasm-fuel-quota-schema.md); [M02 task](./milestones/M02-reliable-node/task.md) |
| Full admin surface | Deferred across M5/M7. | M05 / M07 | [M02 task](./milestones/M02-reliable-node/task.md) |
| Raspberry Pi 4 DB-open perf budget | Perf figure outstanding, tracked with Model B. | blocks-prod | [ADR-0006](../decisions/0006-sqlite-encryption-sqlcipher.md); [M04A B6 plan](./milestones/M04A-proxy-and-auth-foundation/plans/B6.md) |
| **Durable delivery for certificate-bearing supervisor actions** | [M05B](./milestones/M05B-async-primitives/task.md) slice B1 makes **only `write_bindings`** queueable. `apply_plan` and `renew_cert` both embed an instance certificate — `deployment-plan`'s service record carries `instance-certificate: option<string>`, `renew-cert` takes one outright — and `renewed_cert_expires_hours` defaults to **4**. A queued delivery arriving after that window installs an already-dead certificate: the substrate accepts it (well-formed, correctly signed) and the service then fails its handshake closed, so the failure *looks like success*. Making these durable is a different design rather than more of the same one: the queue must store **intent** (service id, generation) instead of the payload and re-mint the certificate at delivery time, which needs the supervisor's vault open at delivery time -- and the vault is locked after every restart until a human runs `inject-kek` (see the vault row above). Deliberately narrowed rather than attempted, because inheriting it silently into B1 is how a slice grows an order of magnitude. Note this loses nothing A6 asked for: A6's scope is durable **push** delivery, and the binding push carries no certificate. **Pickup trigger: a deploy or renewal to an offline substrate is observed to matter in practice** -- today the resident loop's next pass retries both. | TBD | [M05B slice B1 plan](./milestones/M05B-async-primitives/slice-b1-implementation-plan.md) §0.11, D-B1-12; `crates/wit_interfaces/wit/control-plane/control-plane.wit` (`deployment-plan`, `renew-cert`); `crates/core/src/config.rs` (`default_supervisor_renewed_cert_expires_hours`) |
| **The queued-delivery terminal decision narrows on error message text, not a dedicated wire code** | M05B B1's post-landing review (2026-08-05, finding 10) found `deliver_queued_item` treating every reached-and-answered `write-bindings` failure as failure-matrix row 9's "target no longer exists" case — `deploy::is_callee_error` is `true` for *any* `JsonRpcError`, and `control_plane`'s `write-bindings` dispatch maps every server-side refusal (stale generation, authorization gap, genuinely gone service) to the same wire-level `InternalError` (-32603) code. Fixed with `deploy::is_target_gone_error`, which narrows to the one exact message `write_bindings_impl` emits for "gone" ("has no app context on this substrate") — correct today, but a message-text match is brittle: a future wording change to that one error string silently widens every other callee error back into premature dead-lettering, with no compile-time or type-level signal that the two are coupled. The real fix is a dedicated error code (or a typed `JsonRpcError.data` field) `control_plane` emits specifically for "this write's own target is gone," which needs deciding how much of that dispatch's error taxonomy to make wire-visible — out of scope for a review response. **Pickup trigger: `write_bindings_impl`'s error message wording changes.** *(The former second trigger -- "B2's guest-facing proxy DLQ re-derives the same distinction" -- is discharged: [M05B slice B2](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.10/D-B2-11 re-derived it and reached a different answer. On the guest path every callee error dead-letters except one reserved in-flight code, so B2 needs no message match and adds no second instance of this fragility. The `control_plane` taxonomy question below is untouched by it.)* | TBD | `crates/sdk/src/deploy.rs` (`is_target_gone_error`); `crates/control_plane/src/service/orchestration.rs` (`write_bindings_impl`) |
| **A guest cannot read its own dead letters** | [M05B](./milestones/M05B-async-primitives/task.md) slice B2 gives an *operator* three verbs (`svc proxy-outbox`/`proxy-dead-letters`/`proxy-replay`) but gives the calling service nothing: a guest that enqueued a message and had it dead-lettered ten hours later has no way to learn that, so a chat client cannot show "this message was never delivered". Deliberately not built here — it needs guest-facing WIT surface beyond the one field and one function this milestone committed to, and inventing a read API with no consumer is how an interface acquires a shape nobody wanted. The substrate side is not the gap: the record exists and is listable; only the guest's own read path is missing. **Pickup trigger: M6's chat SynApp needs a user-visible failed-send state.** | M06 | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.7; `crates/wit_interfaces/wit/proxy/proxy.wit` |
| **No per-call delivery deadline on `call-options`** | [ADR-0023](../decisions/0023-durable-async-primitives.md) §7 and [M05B task.md](./milestones/M05B-async-primitives/task.md)'s migration section both promise `call-options` gains **two** fields and name only one (`idempotency-key`). B2 shipped that one and did not invent the second. A `deliver-by` deadline is the plausible candidate and is the guest-side answer to the payload-staleness problem B1 hit with certificates: a queued call has a ~10-hour attempt budget, and for some payloads a delivery at hour nine is worse than no delivery at all — the caller is the only party that knows which. Not guessed at here, because a staleness rule with no consumer is a field every guest must reason about and none can use correctly. **Pickup trigger: a consumer needs a queued call to stop being worth delivering** — chat's "don't send this if it is older than N minutes" is the likely first. | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) D-B2-17, §5; `crates/wit_interfaces/wit/proxy/proxy.wit` (`call-options`) |
| **A substrate has no alert store, so a proxy dead letter raises no alert** | [M05B task.md](./milestones/M05B-async-primitives/task.md)'s failure-matrix row 4 says terminal failure "raises an alert". B1 met that through `AlertStore`, which belongs to the **supervisor's** database and is keyed `(instance_id, logical_ref, substrate_did, kind)`. A proxy dead letter belongs to a deployed service on a substrate that may run no supervisor at all, and none of that key exists for it. B2 answers with metrics, a warning log, and a listing verb instead — deliberately, because inventing a substrate-side alert store here would stand up a second alerting model competing with the supervisor's, for one condition. **Pickup trigger: a second substrate-side condition needs operator-visible, clearable state** — then it is worth one store, not two mechanisms. | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.7, D-B2-13; `crates/app_orchestration/src/alerts.rs` |
| **An undeployed service's queue file and dedup records are never removed** | Nothing in the tree removes a service's data directory: there is no `remove_dir_all` anywhere in `data_db` or `control_plane`, so `state.db` already survives an undeploy deliberately, and [M05B](./milestones/M05B-async-primitives/task.md) slice B2's `async.db` inherits that — including a queue of calls the service can no longer make. Handled at the *behavior* level rather than by deletion: the outbox worker only drains services the endpoint registry still knows, and items belonging to a service that is gone are completed silently (delivering would resurrect intent an operator withdrew; dead-lettering would raise noise nobody will act on). What is left is the orphaned file itself, consistent with `state.db`'s existing behavior. B2 deliberately does not invent a deletion path, since the right one deletes both databases together. **Pickup trigger: a service-data removal verb exists.** | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.13, D-B2-15; `crates/router/src/proxy_outbox.rs` (`drain_outboxes_once`) |
| **B1's planning-doc ids in code and WIT comments** | About forty sites across five files cite `M05B Slice B1`, `D-B1-5`, `M05B B1 review finding 4` and similar in code comments, doc comments and WIT comments — exactly what [AGENTS.md](../../AGENTS.md)'s *No Planning-Doc References in Code* rule forbids, since these plans get archived and renumbered and the comment then points at nothing. B2 writes none of its own and cleaned the lines it actually edited (`proxy.wit`'s header, its `protocol` field, the two proxy retry-loop markers), but did not sweep the rest: a slice should not carry a forty-site rewrite of another slice's files, and a behavioral change mixed with a comment sweep is harder to review than either alone. **Pickup trigger: the next non-behavioral pass over `async_queue`/`app_supervisor`, or milestone closeout, whichever comes first.** | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.15; `crates/async_queue/src/lib.rs`; `crates/app_supervisor/src/outbox.rs`; `crates/app_supervisor/src/service.rs`; `crates/sdk/src/deploy.rs`; `crates/wit_interfaces/wit/supervisor/supervisor.wit` |
| **The HTTP bridge has no idempotency path of its own** | Corrected during [M05B](./milestones/M05B-async-primitives/task.md) slice B2's review: HTTP-bridge traffic **already reaches** the receiver-side dedup guard, because `handle_json_rpc_bridge` and `dispatch_native` both forward into `dispatch_json_rpc_once`, and the guard reads the request *body*. An external client that puts `idempotency_key` in its JSON-RPC body therefore gets the same semantics as a guest, fail-closed refusal included. What does **not** exist is an HTTP-native way to express the key (a header, a documented contract, an idempotent-POST story), or an outbox for a client that has no deployed service on this node to own a queue. That is a separate ingress design with no consumer today. **Pickup trigger: an external client needs safe retries against a substrate.** | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.6, D-B2-7; `crates/router/src/route_handler/http.rs` |
| **A service with no unexpired instance certificate cannot use the durable path** | [M05B](./milestones/M05B-async-primitives/task.md) slice B2's `enqueue` refuses outright when the calling service holds no unexpired instance certificate (D-B2-20). The reasoning is that every delivery attempt would present as anonymous, and the receiver refuses a keyed call it cannot scope to a caller — so queuing it would mean ten hours of retries to deliver the same "no", quietly, instead of immediately and loudly to a live guest. Ordinarily invisible, because the supervisor renews certificates unattended; but on a node running deployed services with **no** certificate-renewing supervisor, a service can reach this state permanently and simply cannot use durable delivery at all. A certificate that expires *while* an item waits is correctly treated as a retry, not a terminal failure, so this is only about the state at enqueue time. **Pickup trigger: a deployment runs deployed services with no certificate-renewing supervisor, or an offline-first identity story makes a durable call possible without one.** | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.16, D-B2-20; `crates/router/src/proxy.rs` (`enqueue_call`) |
| **Long-running tasks and their restart rules (`[PLT-ASY]`)** | Deferred out of [M05B](./milestones/M05B-async-primitives/task.md) on 2026-08-04, as its slice B5, decided with the requester before any code was written. It is the only one of item 1's five mechanisms with **no consumer and no near-term consumer**: M6 depends on durable delivery, not on tasks. **The cost is understated by the item text, which is why this row carries the number**: a long-running task is not merely unbuilt, it is structurally prevented — `dispatch_epoch_timeout_secs` defaults to **5 seconds** and bounds every guest entry point ("tight by design: this is the hot path a stuck or hostile guest would otherwise hang forever"), with `lifecycle_hook_epoch_timeout_secs` (30s) the only relief valve and scoped to `init`/`migrate`. So this is a **third guest invocation path** with its own budget and concurrency accounting, plus a durable intent record; the restart rules the requirements spec describes are the small part on top. Safe to defer because the one dependency in the area points the other way — B5's failure branch is defined in terms of B4's compensations, not the reverse — so sagas are unaffected. **Pickup trigger: M5's final phase, with items 2-4.** | M05 (final phase) | [M05B implementation-plan.md](./milestones/M05B-async-primitives/implementation-plan.md) §0.5, D-B-2; [ADR-0023](../decisions/0023-durable-async-primitives.md) §7; `crates/core/src/config.rs` (`default_dispatch_epoch_timeout_secs`) |
| **No unused-dependency check in the build pipeline** | Four unused dependencies accumulated unnoticed and are removed in [M05B](./milestones/M05B-async-primitives/slice-b1-implementation-plan.md) slice B1 phase 1 (`quinn` in `syneroym-router`, `metrics-util` in `syneroym-observability`, `syneroym-sdk` in `syneroym-smoke-tests`, `chromiumoxide` in `syneroym-substrate`'s dev-dependencies) — plus the two `[workspace.dependencies]` entries `metrics-util`/`chromiumoxide` orphan. The plan's own §0.10 sweep also checked `assert_cmd`/`sysinfo` in `syneroym-perf` and `metrics` in `syneroym-sandbox-podman`; both have other consumers and stay, so the implemented removal is four dependencies, not the six the sweep initially reviewed. Nothing stops the same drift recurring: `mise.toml` has no `cargo-machete`/`cargo-udeps` task. Deliberately **not** folded into that chore — adding a lint tool means deciding whether CI fails on it, and whether a false positive blocks a merge, which is a larger decision than removing four known-dead entries. AGENTS.md's "if new tools are needed in the build pipeline, add them to `mise.toml`" rule is the reason this is tracked rather than dropped. | TBD | [M05B slice B1 plan](./milestones/M05B-async-primitives/slice-b1-implementation-plan.md) §0.10, D-B1-10; `mise.toml` |
| **App Supervisor: the resident loop's own shutdown latency is unbounded in the number of managed instances and unreachable substrates** | `AppSupervisor::run` only checks its cancellation token between passes, and one pass walks every active instance sequentially, each up to `MANAGED_SUBSTRATE_CONNECT_TIMEOUT` (10s) per unreachable substrate. `RuntimeServices::shutdown` awaits the loop's `JoinHandle`, so many instances each with a down substrate is a slow shutdown (e.g. twenty such instances is a three-minute wait). Correctness (D-A5c-8's not-dropped-mid-pass property) is right; a bound on how long "finish the pass" is allowed to take is not built. A5c code review finding B-2, signed off rather than fixed. **No longer true of the queue worker**, which shared this row's text until the M05B B1 post-landing review (2026-08-05, finding 5) found `run_queue_worker` did not actually race cancellation against a delivery in flight — only against the next `interval.tick()` — so a claimed batch of up to `QUEUE_WORKER_CLAIM_LIMIT` (100) items, each up to `MANAGED_SUBSTRATE_CONNECT_TIMEOUT`, ran to completion regardless of `shutdown`. Fixed: `queue_worker_tick` now checks cancellation between every item and races each delivery itself inside a `tokio::select!`, so cancellation interrupts an in-flight connect immediately, dropping the abandoned item back to `Pending` via its own visibility timeout (D-B1-8's original intent, now actually true). This row now covers only the resident loop's own pass, not the queue worker. | post-M5 | [M05A Slice A5c](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) review 2026-08-02; `crates/app_supervisor/src/service.rs` (`run`, `run_pass`); `crates/substrate/src/runtime.rs` (`shutdown`) |
| **App Supervisor: a paused instance gets no certificate renewal or anchor refresh, which A5d's 4-hour default turns into a real clock** | `all_active` (the resident loop's own work list) excludes a paused instance entirely, so a paused instance gets no health poll, no renewal, and no master-anchor refresh — the same "and nothing else" `pause`'s own doc already promises. Under A5c's 24-hour certificates this cost little (an operator had a full day to notice and reissue by hand); under A5d's `renewed_cert_expires_hours = 4` default, every member of a paused instance fails its handshake closed within about 4 hours and its master anchors stop verifying within 24 — silently, since the alert passes that would name it never run either. **A5D-03 review, decided against a code fix**: `pause` deliberately does not call `release_on_every_substrate` the way `retire` does (the supervisor still nominally owns a paused instance's substrates, unlike a retired one), so the review's "keep renewing while paused" option is plausible in principle — but it would mean splitting `apply_write_phase`'s single write gate into "maintenance" (renewal, anchor refresh) and "reconcile" (needs-work redeploys, restarts) sub-phases, reversing a documented, tested A5c design decision (`the_loop_skips_paused_and_retired_instances`, `a_loop_pass_stamps_last_reconciled_at_but_a_skipped_instance_is_untouched` — both assert a paused instance gets *zero* processing, not even a health poll, and say so explicitly) with no strong evidence that decision was wrong versus just newly consequential now that `renewed_cert_expires_hours` dropped from 24h to 4h. Documented instead (`docs/developer-guide.md`'s `pause`/`resume` section now states the consequence plainly), not fixed. | TBD | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) Part V (§26-§32); `crates/app_supervisor/src/store.rs` (`all_active`); `crates/app_supervisor/src/service.rs` (`reconcile_instance_pass`, `apply_write_phase`); `docs/developer-guide.md` (`pause`/`resume`) |
| **App Supervisor: nothing pins which `CertAlertPolicy` the resident loop and `handle_status` actually pass** | The two `record_report` unit tests (`managed_elsewhere_raises_neither_cert_alert_kind`, `managed_elsewhere_does_not_clear_a_cert_alert_the_caller_raised`) prove `CertAlertPolicy::ManagedElsewhere` behaves correctly wherever it is passed; neither proves the supervisor's own two call sites still pass it. A supervisor pass-level test that drives a near-expiry certificate through a real `reconcile_instance_pass` and asserts no `CertificateNearExpiry` row would close that gap directly — and cannot be built: `renewed_cert_expires_hours` is whole-hours-granularity (`u64`), so the shortest near-expiry window a real config can produce is ~15 minutes into a 1-hour certificate, impractical for a test to wait out; and mint-in-place custody ([ADR-0020](../decisions/0020-stable-logical-service-identity.md) §3) means the member master never leaves the supervisor's vault, so no test can forge a near-expiry certificate for a supervisor-managed member from outside it either. **Mitigated, not closed**: both call sites now read one named `SUPERVISOR_CERT_ALERT_POLICY` constant instead of two independent `CertAlertPolicy::ManagedElsewhere` literals, so the two call sites drifting apart — which is how A5D-01 went undetected the first time — can no longer happen silently; an edit to the constant's own value still has no automated trip-wire. A sub-hour test-only renewal cadence (or an equivalent seam) would need its own design decision before this closes for real. | TBD | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) Part V (§26-§32); `crates/app_supervisor/src/service.rs` (`SUPERVISOR_CERT_ALERT_POLICY`); `crates/sdk/src/health.rs` (`CertAlertPolicy`) |
| **App Supervisor: `instance_locks` grows without bound, keyed on caller-supplied ids** | `instance_lock` inserts an entry on first use and nothing ever removes one -- not on `retire`, not on a pass that finds the instance gone. `adopt`/`release`/`retire`/`force-reconcile` all take the lock before checking the instance exists, so an admin caller can grow the map by calling any of them with arbitrary ids. A5c code review finding B-3, signed off rather than fixed: admin-only and small per entry, housekeeping rather than a hazard at today's scale. | TBD | [M05A Slice A5c](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) review 2026-08-02; `crates/app_supervisor/src/service.rs` (`instance_lock`) |
| **App Supervisor: single-writer lease / supervisor HA** | One supervisor per app instance, enforced by an owner+generation stamp on the instance record ([ADR-0021](../decisions/0021-binding-propagation-and-app-supervisor.md) §4) — sufficient to stop two supervisors flapping, but it is not HA: if the supervisor's node is lost, reconciliation stops until it is rebuilt. A real lease needs M5's cron-lease primitive; replicated supervisor state needs M7. Mitigated by design rather than by redundancy: desired state is rebuildable from the operator's manifests plus a sweep of the target substrates, so losing the node costs a rebuild, not the app. **Retargeted M07 on 2026-08-04, and the "needs M5's cron-lease primitive" clause is withdrawn**: [M05B](./milestones/M05B-async-primitives/task.md) builds no lease, because there is nothing in this topology for one to arbitrate — registry writes are partitioned by key ownership, so distinct principals never contend, and inside an app instance the generation stamp already decides the writer deterministically. HA needs replicated supervisor state, which is M7 regardless of any lease. | M07 | [ADR-0023](../decisions/0023-durable-async-primitives.md) §6; [ADR-0021](../decisions/0021-binding-propagation-and-app-supervisor.md) §4; [M05B implementation-plan.md](./milestones/M05B-async-primitives/implementation-plan.md) §0.4 |
| **App Supervisor: remediation by relocating a stateful service** | Remediation is **restart-in-place only**. [ADR-0020](../decisions/0020-stable-logical-service-identity.md) removes the identity blocker (a moved service keeps its master DID, so FDAE policy, stored row attribution, and grants all survive), but a stateful service's SQLite lives on its substrate and cannot follow it until replication exists. Relocating a stateless service is possible sooner, except that nothing in the manifest currently declares which services are stateless — that field is unbuilt too. | M07 | [ADR-0020](../decisions/0020-stable-logical-service-identity.md); [M05A task.md](./milestones/M05A-app-supervisor/task.md) non-goals |
| **App Supervisor: pull-side app directory** | Deliberately not built ([ADR-0021](../decisions/0021-binding-propagation-and-app-supervisor.md) §6). Re-addable as a second `AppRegistry` implementation with no redesign — the trait, cache, epoch, and TTL all already exist for one. **Revisit trigger is measured, not aesthetic:** if binding convergence across dependents cannot be held inside its budget by delivery retry alone, build the pull path. M05A's exit criteria require that measurement and an explicit written answer either way. | TBD (trigger-driven) | [ADR-0021](../decisions/0021-binding-propagation-and-app-supervisor.md) §6; [M05A task.md](./milestones/M05A-app-supervisor/task.md) exit criteria |
| **App Supervisor: alerting transports** | Alerts are emitted as structured events, queryable, and published to an MQTT topic (the broker is already in-process). No SMTP, webhook, or paging delivery — that belongs in a separate consumer service, not in the supervisor. | TBD | [M05A task.md](./milestones/M05A-app-supervisor/task.md) non-goals |
| **Member master key custody, escrow, and loss** | [ADR-0020](../decisions/0020-stable-logical-service-identity.md) §4 makes a member's master DID the thing FDAE policy evaluates against **and** the thing stored rows carry as creator/owner attribution. So losing a member master key is unrecoverable in a way that ordinary key loss is not: no further instance of that member can be certified, and any replacement necessarily gets a different master DID, orphaning it from every row the old member wrote. No policy edit repairs it — the old DID is in the data. M05A ships with backup as a documented operator duty and nothing more: no escrow, no master rotation, and no re-attribution path for a lost master. Distinct from the person/organization master-key row above (M10+), which is about human identity and says nothing about member masters or the orphaned-data consequence. Slice A0's only planned mitigation is a **mint-time warning** on the deploy path (which ADR-0020 §4 explicitly asks for); the keys themselves are ordinary `roymctl` identities at `<roymctl --dir>/identities/member-<app_instance_id>#<service_name>-<index>.key` (M05A A5e changed the `app_instance_id`/`service_name` boundary from `-` to `#`, D-A5e-12), written `0o600` by `Identity::save_to_path`. **2026-07-28 proposal, evaluated and not pursued for now:** deterministic HD derivation of every member master from one operator root key via `HKDF(RootKey, AppInstanceId, LogicalServiceName, MemberIndex)`, so recovery needs only the root key plus the (already git-tracked) manifest instead of a live backup of each `.key` file. Rejected for the present because it trades away, rather than preserves, the property this row exists to protect: under the attended posture (master keys held only on the operator's own machine, the common case before A5's online-key posture), one compromised or leaked root key derives every past *and future* member master under it, inverting ADR-0020 §3's per-member blast-radius bound ("compromising a member master lets an attacker impersonate that one member, not the logical service"). It also reintroduces, at rename time, the same orphaning failure §7 rejected for relocation: a `LogicalServiceName` edit changes the HKDF input and mints a new master DID, orphaning the member's own stored data with no policy fix available. Changing the trust model this way would need its own ADR (with that blast-radius tradeoff argued explicitly) before any code change — not attempted here. Revisit only after M05A ships, and treat this row as the design's home rather than opening a new one. | TBD | [ADR-0020](../decisions/0020-stable-logical-service-identity.md) §4; [M05A task.md](./milestones/M05A-app-supervisor/task.md) non-goals; [M05A slice A0 plan](./milestones/M05A-app-supervisor/slice-a0-implementation-plan.md) §1 D-A0-2/D-A0-5 |
| **App Supervisor: placement is declared, not scheduled** | v1 placement is a global default plus per-service override, by substrate **alias**, resolved against the substrate inventory. No substrate pools, no capability- or load-based placement, no constraint solving. The manifest field is a selector from day one so adding variants later does not reshape the schema — shipped as designed in A3 (`PlacementSelector`, an externally-tagged enum with one variant, `Substrate(SubstrateAlias)`). | TBD | [M05A task.md](./milestones/M05A-app-supervisor/task.md) Slice A3; [M05A slice A3 plan](./milestones/M05A-app-supervisor/slice-a3-implementation-plan.md) D-A3-1 |
| **No remote `ControllerAgreement` claim** | `roymctl substrate claim` needs both the controller's and the node's private keys, and the node's key lives only on the node's own filesystem — no RPC returns a signature over caller-supplied bytes. So the tool is inherently local and offline; provisioning N substrates for A3 means visiting N hosts (or shipping `agreement.json` out of band). A TOFU (first-claim-wins) remote claim endpoint is the alternative and was not built. **A3 is the consumer that makes this felt**: every substrate named in the deploy inventory must be claimed on its own host before `roymctl app deploy` can reach it. | TBD | [M05A slice P0 plan](./milestones/M05A-app-supervisor/slice-p0-implementation-plan.md) §0.8; [M05A slice A3 plan](./milestones/M05A-app-supervisor/slice-a3-implementation-plan.md) §11; `apps/roymctl/src/commands/substrate.rs` (`Claim`) |
| **Claiming a substrate requires a restart** | `setup_substrate_identity` reads the agreement once, at boot; there is no reload path (unlike TLS's `SIGUSR1` hot-reload). Claiming a *running* node needs a restart to take effect. | TBD | [M05A slice P0 plan](./milestones/M05A-app-supervisor/slice-p0-implementation-plan.md) §8; `crates/substrate/src/identity.rs` |
| **A3: a redeploy that moves a service to a different substrate is refused, not relocated** | D-A3-12: the naive behavior (deploy to the new substrate, leave the old instance running) creates a silent two-publisher state — the old instance keeps running and keeps republishing its endpoint record, exactly the flap the *"relocated-away substrate"* row above describes. A3 detects this (sourced from `COMPLETED` action rows across every journal record for the instance, D-A3-22) and fails with an explicit error naming both substrates. **Post-review fix:** the error's own recovery steps used to be incomplete — `svc remove` undeploys the running instance but has no concept of an app instance or a journal, so the `COMPLETED` row survived it and every later `app deploy` hit the identical refusal forever, with no escape short of deleting `deployments.db`. `roymctl app forget <instance> --service <name>` now closes it: it appends a `REMOVE` row for the service's most recent placement (no substrate contact), and `check_no_placement_change` looks at the *most recent* row for a logical ref, of either action type, rather than the most recent `ADD` alone — so a `REMOVE` clears the refusal. The error message names both steps now. Emitting `Remove` + `Add` as a real relocation (rather than refuse-then-forget) needs the old substrate's client, an `undeploy` capability there, and an ordering rule — more work than it looks, and "no relocation" is already a milestone non-goal through A4. **Retargeted to post-M5**: A5a-A5e keep "no relocation" as a non-goal too — the supervisor built this milestone manages a service's lifecycle on its existing substrate, not cross-substrate moves. | post-M5 | [M05A slice A3 plan](./milestones/M05A-app-supervisor/slice-a3-implementation-plan.md) §0.5, D-A3-12; [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §17; `apps/roymctl/src/commands/app.rs` (`check_no_placement_change`, `AppCommands::Forget`) |
| **`roymctl app deploy` discards every compiled plan but the last, so a `Spawn` dependency's services are never deployed** | `compile` pushes a spawned child's plan before the root's, but `app deploy` only ever takes `compiled.plans.last()` — a `Spawn` dependency's whole plan is computed and thrown away. Predates A3; makes A3's own root-placement-cascades-into-spawned-children behavior (D-A3-3) correct but currently unobservable, since no operator can reach a child plan to deploy it. Fixing this needs cross-plan binding wiring nothing has asked for yet. | TBD | [M05A slice A3 plan](./milestones/M05A-app-supervisor/slice-a3-implementation-plan.md) §0.13; `apps/roymctl/src/commands/app.rs`; `crates/app_orchestration/src/compiler.rs` |
| **A3: `app::handle`'s own CLI-level orchestration has no live e2e test coverage** | **Half-closed by A5a**: `roymctl` is now split into `lib.rs` + `main.rs` (`apps/roymctl/src/lib.rs`), so `commands::app::handle` (and `commands::svc::handle`) are linkable from a `crates/substrate` e2e test — the blocker this row named is gone. What A5a does **not** add is the e2e test itself that calls `handle` against a real two-node `Node` fixture with a `substrates.toml`; `binding_push_e2e.rs` and `multi_substrate_placement_e2e.rs` still call `deploy::apply_plan`/`SyneroymClient` directly, mirroring `roymctl` rather than driving it. That coverage is A5b's to add (its own `roymctl supervisor` commands will need the same linkability, so the natural point to also cover `app::handle` live is alongside them). | A5b | [M05A slice A3 plan](./milestones/M05A-app-supervisor/slice-a3-implementation-plan.md) post-merge review, finding 05; [M05A slice A4 plan](./milestones/M05A-app-supervisor/slice-a4-implementation-plan.md) §11; [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §4.4, D-A5-14; `apps/roymctl/src/lib.rs`; `crates/substrate/tests/multi_substrate_placement_e2e.rs` |
| **`svc restart` cannot restart a `tcp` service** | `restart_impl` only knows how to reload a wasm component or restart a container; a plain `tcp` service is a process the substrate does not manage the lifecycle of, so `restart` on one returns an error by construction. A5a's only planned remediation for a stuck `tcp` service is alerting, not restarting it. | TBD | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §4.3, §17; `crates/control_plane/src/service/orchestration.rs` (`restart_impl`) |
| **`app_instance_management` replaces `app_instance_owners` with no migration** | A5a drops the `app_instance_owners` SQLite table outright and replaces it with `app_instance_management`, per pre-release policy (no `ALTER TABLE`, no version ladder). A substrate upgrading past this commit loses its app-instance ownership rows; the next deploy against each affected `service_id` re-establishes ownership first-write-wins, same as any other pre-release schema change. Recorded as a known upgrade effect, not a task. | n/a (resolved by design) | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §5.2, D-A5-6, §17; `crates/data_db/src/registry_store.rs` |
| **A4: `SubstrateEndpoint` cannot distinguish a container from a TCP service** | Container deploy and TCP deploy both register `SubstrateEndpoint::TcpHostPort` (`orchestration.rs`); `PodmanSocket` is never registered by orchestration at all, so `list_impl`'s `"podman"` arm and `DeployedService.endpoint_type`'s container case are both unreachable. A4 works around it by persisting the service type separately (`service_deploy_facts`) rather than fixing the variant. | TBD | [M05A slice A4 plan](./milestones/M05A-app-supervisor/slice-a4-implementation-plan.md) §0.5; `crates/core/src/local_registry.rs` (`SubstrateEndpoint`) |
| **`--no-default-features` does not build `syneroym-control-plane`** | `crates/control_plane/src/dummy_sandbox.rs`'s no-feature stubs are incomplete (`ContainerEngine` had no methods at all before A4 added `readyz`), while `orchestration.rs` calls `AppSandboxEngine::{deploy_wasm,stop_wasm,remove_wasm,unsubscribe_all}` and the container engine's `deploy`/`stop`/`remove`/`readyz` unconditionally. Predates A4; the gates run `--all-features`, so it is never exercised. | TBD | [M05A slice A4 plan](./milestones/M05A-app-supervisor/slice-a4-implementation-plan.md) §0.10; `crates/control_plane/src/dummy_sandbox.rs` |
| **`svc deploy` cannot declare a health check** | A standalone `svc deploy` builds its `DeployManifest` directly and always sends `health_check: None` — no CLI flag exists. A probe belongs to an app's manifest, which is the thing a supervisor manages, so this is an intentional gap rather than an oversight, but it means a standalone (non-app) TCP/container service deployed via `svc deploy` gets no A4 probe coverage. | TBD | [M05A slice A4 plan](./milestones/M05A-app-supervisor/slice-a4-implementation-plan.md) §2.7; `apps/roymctl/src/commands/svc.rs` |
| **A4: an `rpc` probe against a method with a required parameter permanently fails** | `run_probe`'s `Rpc` arm always sends `params: Value::Array(vec![])` -- `json_to_wasm_params`'s `default_for_missing` (`sandbox_wasm/src/conversions.rs`) returns `Ok` only for an `Option`-typed parameter the array doesn't supply, erroring for anything else. A probe method taking a required argument (post-review finding N-1) is therefore a permanent `ProbeFailing`, indistinguishable at the supervisor from a real outage, with no deploy-time check to catch it. Not fixed here: the real fix is either a `params` field on the WIT `rpc-probe` record (a schema decision -- JSON object vs. positional array, threaded through `sdk`'s mapper and the app-model `RpcProbe` too) or deploy-time introspection of the guest's exported function signature (extending D-A4-6's `valid_for` check, which today is a static kind/service-type pairing with no wasm component inspection at all) -- either is a real design change, not a drive-by patch. Pinned explicitly rather than left as an accidentally-green test: `an_rpc_probe_permanently_fails_for_a_method_that_takes_a_required_argument` asserts on the failure, and `an_rpc_probe_passes_for_a_method_that_takes_no_arguments` proves the happy path with a zero-argument method instead. | TBD | `crates/control_plane/src/service/orchestration.rs` (`run_probe`); `crates/sandbox_wasm/src/conversions.rs` (`default_for_missing`); `crates/wit_interfaces/wit/control-plane/control-plane.wit` (`rpc-probe`) |
| **`test_wit_adherence` only catches a missing dispatch arm, never an extra one** | Walks the parsed WIT `orchestrator` interface's own function list and asserts each has a matching dispatch arm -- one direction only. A dispatch arm with no WIT counterpart (exactly how A4-06's `node-facts-only` shipped without a WIT declaration for one review round, N-2) passes it silently. Not fixed in the same pass that found it: making it bidirectional needs the dispatch table's method strings to exist somewhere enumerable and checkable against, not just as literal `match` arm patterns, which is a small refactor of `service.rs`'s dispatch table, not a one-line test change. Worth doing before A5 adds its own `supervisor` interface and dispatch arms, so the same silent-drift window doesn't reopen there. | A5 | `crates/control_plane/src/service.rs` (`test_wit_adherence`, the dispatch `match` in `NativeService::dispatch`) |
| **A `ProbeFailing` service is never restarted, and `HealthCheck` has no initial delay or failure threshold** | A5c's remediation loop restarts a landed `InstanceNotRunning` service, never a `ProbeFailing` one (D-A5c-17, §21): there is no way to tell "still starting" from "genuinely broken" without an `initial_delay_secs`/`failure_threshold`-shaped field on `TcpProbe`/`HttpProbe`/`RpcProbe`, none of which exist, and remediation would also need per-service-type gating since `restart` is refused outright for `tcp` and `nativehost`. | post-M5 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §21, D-A5c-17; `crates/app_orchestration/src/models.rs` (`HealthCheck`, `TcpProbe`/`HttpProbe`/`RpcProbe`) |
| **`ReconcileAction::Remove` has no executor anywhere in the tree** | `compute_diff` produces a `Remove` action when a resubmitted plan drops a service, and `apply_plan` iterates only `Add`/`Update` actions -- nothing undeploys the dropped service, ever. A5c's loop alerts on it (`OrphanedService`, D-A5c-3/D-A5c-21) rather than undeploying: a stateful service dropped by a manifest edit being torn down automatically is destructive, and no policy exists yet for when that would be safe. | TBD, needs a destructive-action policy first | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §19.2a, D-A5c-3; `crates/app_orchestration/src/reconcile.rs` (`ReconcileAction::Remove`); `crates/sdk/src/deploy.rs` (`apply_plan`) |
| **`messaging/subscribe` needs no capability, only a handshake** | Widened at A5c (§19.5e/F10) from a narrower A4-era wording ("MQTT alert topics are readable by any verified caller"): `messaging/subscribe` gates only on the caller not being anonymous, and `namespace_topic`'s subscribe-side cross-service opt-in (a literal `svc/` prefix passed through unchanged) means **any** `messaging` endpoint on a node is a subscribe handle for **any** `svc/<id>/#` topic on it, not only the topic that endpoint belongs to -- pre-existing on any node hosting deployed services. A5c avoids widening this to supervisor-only nodes by namespacing the supervisor's own subscribe path with the unconditional publish-side rule instead (D-A5c-6), but the underlying missing capability gate (a `messaging/subscribe`-shaped ability, pairing with the existing `supervisor/status` ability row above) is still open. | post-M5 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §19.5e, D-A5c-6; `crates/router/src/route_handler/dispatch.rs:350`; `crates/mqtt_broker/src/lib.rs` (`namespace_topic`) |
| **wasmtime 47.x+ was investigated, not adopted, for A5c's probe-concurrency fix** | The 20-service poll-cost budget test (D-A5c-12) failed under concurrent health probes at the default `max_concurrent_instances: 10` -- fixed with a semaphore bounding concurrent probe instantiations (`AppSandboxEngine::probe_instance_permits`), the user's explicit choice this session over exploring whether a wasmtime 47.x+ upgrade changes the pooling-allocator behavior enough to avoid needing one. Not attempted; the semaphore fix ships instead. | post-M5 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §19.13, D-A5c-12; `crates/sandbox_wasm/src/engine.rs` (`probe_instance_permits`, `execute_probe_json`) |
| **`probe_cached` has no single-flight, only mitigated by the concurrency-limiting semaphore above** | Post-review correction (A5c code review, 2026-08-02): this row was previously marked resolved by D-A5c-12's semaphore, which is wrong -- the semaphore bounds how many probes may instantiate a wasm component *at once*, a real and separate fix for the pool-exhaustion bug it was built for, but `probe_cached` (`crates/control_plane/src/service/orchestration.rs`) is unchanged: read the cache, miss, run the probe, write the cache, with no in-flight dedup between the read and the write. Two concurrent callers racing the same cache miss (e.g. `status` and a loop pass both polling right after `PROBE_MIN_INTERVAL_SECS` expires) still run the probe twice. Mitigated in practice by the semaphore capping how expensive that duplication can get, not eliminated. | TBD | [M05A Slice A5c](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5c-12; `crates/control_plane/src/service/orchestration.rs` (`probe_cached`) |
| **`replicas` places every member of a service on one substrate** | `ServiceSpec.placement` is a single `PlacementSelector`, so scaling a service to N members does not survive losing the node they all share -- the main thing an operator scaling a service would want redundancy for. A5e's placement-change guards (`refuse_placement_change`, `check_no_placement_change`) were fixed to compare a member against its own journal row rather than inheriting a sibling's, so a *future* per-member placement selector would not be misread by them, but no such selector exists yet -- needs a placement-selector design (a list, a spread constraint, or a pool), not a supervisor change. | post-M5 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §33.6, D-A5e-6; `crates/app_orchestration/src/models.rs` (`ServiceSpec.placement`) |
| **`replicas` with a stateful service splits its data across N databases** | Each `replicas` member is its own `service_id` and therefore its own SQLite database (`SqliteStorageProvider` opens one file per `service_id`), while `Redundant` mode round-robins unkeyed calls across them -- reads and writes for one entity would land in different stores. The compiler refuses the combination it can detect (`replicas > 1` alongside a declared `schema`, D-A5e-16), naming M7's `[PLT-RED]` state replication as the reason the refusal will relax; a service that uses the data layer without declaring a `schema` is the refusal's own documented blind spot. Recorded as a known property with an operator-facing warning (`docs/developer-guide.md`'s "Scaling a service" section), not as debt A5e could have paid itself. | M7 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §33.14, D-A5e-16; `crates/app_orchestration/src/models.rs` (`SynAppManifest::validate`); `docs/developer-guide.md` |
| **`TopologyMode::Sharded` is compiled by nothing** | A5e's `replicas` sets `TopologyMode::Redundant` unconditionally above 1 -- `Sharded` needs a `ShardingStrategy` manifest surface (slice **S1** of the Logical Service Discovery Overlay) and a routing key on the wire (slice **S3**) before it is reachable from a manifest at all; the resolver's own `Sharded` arm and its range/rendezvous selection stay exercised by unit tests only. **Named for S1's benefit**, not left as a bare gap: D-A5e-4's derivation is the one line `crates/app_orchestration/src/compiler.rs` (`replicas > 1` ⇒ `Redundant`) that S1 will change to `strategy present ⇒ Sharded, else Redundant` once `ShardingStrategy` lands on the same `ServiceSpec`. | S1 | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §33.4/§37, D-A5e-4; [meta-implementation-plan.md § Committed Work: Logical Service Discovery Overlay](./meta-implementation-plan.md#committed-work-logical-service-discovery-overlay-2026-08-02) slice S1; `crates/app_orchestration/src/compiler.rs` |
| **Scale-down leaves the removed member deployed** | `Reconciler::compute_diff` can name a removed member after A5e's re-key (`ReconcileAction::Remove(MemberRef)`), but the resident loop still does not undeploy it -- a `Remove` is raised as `OrphanedService` and left running (D-A5c-2's rule, unchanged by A5e). Scaling a service from 3 members to 2 leaves the third running and publishing its endpoint record until an operator removes it by hand. Pairs with the existing *"Stale `StaticInventory` entries after undeploy"* row above, which has the same "alert, don't act" shape. | TBD | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §37, D-A5c-2; `crates/app_supervisor/src/service.rs` (`OrphanedService` raise site) |
| **A member removed from the plan keeps its master in the vault** | Nothing forgets a `member-<instance>#<service>-<index>` vault key after a scale-down, so scaling 2 → 1 → 2 silently reuses the old member 1's master (its data and bindings intact) -- which is *correct* today but is not stated as a decision anywhere, and there is no verb to forget one deliberately if an operator ever wanted to. | TBD | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §37; `crates/app_supervisor/src/keys.rs` (`member_master_name`) |
| **The D-A5e-12 vault-key rename re-identifies every member of an already-deployed instance, silently** | `member_master_name`'s `app_instance_id`/`service_name` boundary moved from `-` to `#` to close the collision D-A5e-12 fixes. `mint_and_substitute` resolves a member's master by `get_or_mint`, so an instance deployed before this change finds nothing under the new name on its next `submit` and mints a fresh master per member instead of failing -- every member gets a new DID, `diff_plans` sees it as an `Update` (member ref unchanged, `service_id` is not) and redeploys under the new identity, and the *old* master's anchor and `revoked_placements` rows are left pointing at a DID nothing references any more. `master_for_member` (renewal/revocation) fails outright in the window before that resubmit, since it reads with `get`, not `get_or_mint`. Same shape as the *`app_instance_management` replaces `app_instance_owners`* row above -- an in-place rename with no migration, accepted per pre-release policy -- but with a real consequence in between (orphaned anchor/revocation rows) rather than a clean first-write-wins re-establishment. Documented in `docs/developer-guide.md`'s custody section rather than fixed: `get_or_mint` falling back to the old name once and re-storing under the new one was considered and rejected as unnecessary migration-shim machinery for a pre-release product. | n/a (resolved by design) | M05A Slice A5e code review round 2 (2026-08-04), finding B; `crates/app_supervisor/src/keys.rs` (`member_master_name`, `mint_and_substitute`); `apps/roymctl/src/commands/member_identity.rs`; `docs/developer-guide.md` |
| **No operator surface removes a member's supervisor-side rows, and one of the four re-keyed tables must never lose them** | `supervisor.wit` has no member-removal verb, and `roymctl app forget` reaches only the operator's own `deployments.db`, never the supervisor's `supervisor.db` -- so `remediation`, `pending_rotation_restarts`, `binding_epochs`, and `revoked_placements` each accumulate one row per member that ever existed (bounded by members × instances, mostly inert). A5e builds no forgetting verb, deliberately (D-A5e-18, withdrawn after review finding S1): `binding_epochs` in particular **must not** be cleared while the substrate still holds a binding row for that member -- `advance_binding_epoch` inserts at 1 on a missing row, so a cleared-and-restored epoch would sit permanently below the substrate's held value and every later push would classify `Stale` forever. `revoked_placements` must never be cleared at all (pairs with the `revoke-instance has no path back` row above, D-A5d-15's same reasoning). The other two tables are safe to clear whenever a verb exists. Whoever builds that verb should read this row, and D-A5e-18's rewritten text, before writing it. | TBD | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §33.20/§37, D-A5e-18; `crates/app_supervisor/src/store.rs` (`advance_binding_epoch`, `remediation`, `pending_rotation_restarts`, `revoked_placements`) |
| **`roymctl app forget` has no way to name one member of a scaled service** | `--service` names a logical service, and the command's own hardcoded `index: 0` used to silently forget member 0 alone while its siblings stayed tracked as if nothing had happened -- fixed in review to refuse a service with more than one landed member instead of mis-acting, but the underlying gap (no verb to name a single member) is still open: `refuse_placement_change`'s own error message tells an operator to run `roymctl app forget`, which still cannot clear one member's row on a scaled service. Needs `--service` to accept a `MemberRef` string (`<logical_ref>#<index>`), or a new `--member-index` flag. | TBD | M05A Slice A5e code review (2026-08-04), finding 8; `apps/roymctl/src/commands/app.rs` (`AppCommands::Forget`) |
| **Phase 3 of A5e's own test plan (§36, tests 56-63/72-74/79/81-82) shipped mostly untested** | The re-key of the journal, the alert index, both placement guards, `revoke-instance`, the status read surface, and the `ExpectedService` join all check out on inspection (traced by hand in review), but only 4 of the 14 planned phase-3 cases were written, in response to the review: `master_anchor_refresh_republishes_each_members_own_anchor_and_stamps_its_own_row` (63), `a_second_member_placed_on_a_different_substrate_is_not_refused_as_a_relocation` (59), `restart_attempts_are_counted_per_member_not_per_logical_service` (60), and `a_members_placement_is_found_after_the_journal_is_re_keyed` (82) -- the four the review named highest-value (the silent-total and fail-closed-outage failure modes). Still missing: 56 (`apply_plan`'s resume-skip through two members on one substrate), 57/58 (scale-down/scale-out placement-guard cases), 61 (`binding_convergence_rows` one-row-per-member), 62 (`revoke-instance` scoped to one member), 72/73 (`status`/`submit` reporting a distinct ref and minted row per member), 74 (renewal alert clearing per member), 79 (a forgotten-and-restored member's binding epoch), 81 (all six `ExpectedService` construction sites, only one of which — `handle_status`'s — is now covered by 82). | TBD | [M05A slice A5 plan](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §36 phase 3; M05A Slice A5e code review (2026-08-04), finding 3; `crates/app_supervisor/src/service.rs`, `crates/app_supervisor/src/store.rs`, `crates/sdk/src/deploy.rs` |
| **Nothing forgets an app master, and nothing should** | An `app-<instance>` vault key outlives `retire`, `release`, and every resubmit (D-A7-11), and there is no verb to remove one -- the same standing constraint the *"A member removed from the plan keeps its master in the vault"* row above already carries for member masters, extended here to the one app-level key. Bounded (one key per instance this supervisor has ever adopted) and deliberate: forgetting a key is unrecoverable, keeping one is merely untidy. | TBD | [M05A slice A7 plan](./milestones/M05A-app-supervisor/slice-a7-implementation-plan.md) §0.11/§4, D-A7-11; `crates/app_supervisor/src/keys.rs` (`app_master_name`) |
| **Two supervisors can mint two different app masters for one instance** | Adopting on a second supervisor *before* `import-master` gives the app instance a second identity: `adopt` mints under `app-<id>` when the vault holds nothing yet, and ADR-0021 §4's generation fence does not catch it -- a generation fences two writers over *one* record, and the wrong order produces two DIDs, hence two records that never meet. A7 documents the correct order (`import-master` before `adopt`) and makes `adopt` self-correcting (resolve-and-record on every call, D-A7-5), but does not enforce it. Harmless while nothing publishes the key; a real split-identity bug the moment S1's Tier-1 record does. | S1 | [M05A slice A7 plan](./milestones/M05A-app-supervisor/slice-a7-implementation-plan.md) §0.5/§4, D-A7-5; [ADR-0022](../decisions/0022-two-tier-logical-service-discovery.md) §1's 2026-08-04 amendment; `crates/app_supervisor/src/service.rs` (`handle_adopt`) |
| **The app master has no anchor and no revocation story** | It delegates nothing today (D-A7-10) and its future Tier-1 record will be self-signed under its own DID -- the registry's plain self-signed admission path, not the delegation-signed second path member masters use -- so an anchor is not needed by A7. If S1 or S2 ever wants app-key revocation expressed the way member-key revocation already is (`revoke-instance`, D-A5d-10), that needs its own decision and its own anchor mechanism. Recorded as a known property, not as debt A7 could have paid itself. | S1 | [M05A slice A7 plan](./milestones/M05A-app-supervisor/slice-a7-implementation-plan.md) §0.10/§4, D-A7-10; `crates/app_supervisor/src/keys.rs` |
| **Renewal is skipped whole on a node with `storage.encryption = false`, and misreports why** | `kek_is_loaded()` reads the `KeyStore`, not the storage provider's own encryption flag -- so on a node that turned encryption off (a real, if unusual, config), the vault works perfectly but `kek_is_loaded()` still answers `false`. A5d's renewal work-list uses this as a cheap pre-check (avoiding one failed vault read per due member per pass) and is skipped entirely as a result, raising a `VaultLocked` alert per due member for a vault that would actually have opened. Found while scoping A7's own mint pre-check, which hits the identical trap and avoids it by attempting the mint and reading `VaultError::Locked` instead of pre-checking `kek_is_loaded()` (D-A7-1) -- A7 does not change A5d's existing behavior. The real fix is a "can this vault actually be opened" question, not a "is a KEK loaded" one. | TBD | [M05A slice A7 plan](./milestones/M05A-app-supervisor/slice-a7-implementation-plan.md) §0.1/§4; `crates/app_supervisor/src/service.rs:1026` (renewal work-list gate); `crates/app_supervisor/src/keys.rs` (`kek_is_loaded`) |
| **A guest cannot read its own dead letters (M05B B2, planned)** | The proxy DLQ B2 builds is listable and replayable by an *operator* (`roymctl`), never by the calling service itself -- so a chat client has no way to show "this message was never delivered". Needs guest-facing WIT surface beyond the one `call-options` field plus one function this milestone committed to. Deferred at plan time, not discovered late. | M06 | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.7, D-B2-13. **Pickup trigger:** M6 chat needs a user-visible failed-send state |
| **No per-call delivery deadline on `call-options` (M05B B2, planned)** | [ADR-0023](../decisions/0023-durable-async-primitives.md) §7 and the M05B task doc both promise `call-options` gains *two* fields and name only one (`idempotency-key`). B2 adds that one (D-B2-17). The plausible second is a `deliver-by`/`not-after` deadline letting a caller declare when a queued call stops being worth delivering -- the guest-side answer to the payload-staleness problem B1 hit with instance certificates -- invented here would be a field with no consumer. | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) D-B2-17; `crates/wit_interfaces/wit/proxy/proxy.wit` (`call-options`). **Pickup trigger:** a consumer needs a queued call to expire -- chat's "don't send this if it is older than N minutes" is the likely first |
| **A substrate has no alert store, so a proxy dead letter raises no alert (M05B B2, planned)** | The M05B failure matrix's row 4 requires terminal failure to raise an alert. `AlertStore` is the *supervisor's*, keyed by `(instance, logical_ref, substrate, kind)`, and a deployed service's proxy dead letter has none of that key -- so B2 answers with a metric, a warning log, and a listing verb instead of a clearable alert row. Inventing a substrate-side alert store here would be a second alerting model competing with the supervisor's. | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.7; `crates/app_orchestration/src/alerts.rs`. **Pickup trigger:** a second substrate-side condition needs operator-visible, clearable state |
| **An undeployed service's queue file and dedup records are never removed (M05B B2, planned)** | Nothing in the tree removes `<db_dir>/services/<service_id>/` -- `undeploy` clears endpoint registrations, the owner row, and the instance certificate, but `state.db` already survives it deliberately. B2's per-service `async.db` inherits that, so a queue of calls an undeployed service can no longer make stays on disk. Handled rather than fixed: the worker drains only services the endpoint registry still knows, and completes an orphaned item silently (D-B2-15). | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.13, D-B2-15; `crates/data_db/src/sqlite.rs` (`resolve_service_db_dir`). **Pickup trigger:** a service-data removal verb exists |
| **B1 cites planning-doc ids in code and WIT comments, in about forty places** | `M05B Slice B1`, `D-B1-5`, `M05B B1 review finding 4` and similar appear in `crates/async_queue/src/lib.rs`, `crates/app_supervisor/src/{outbox.rs,service.rs}`, `crates/sdk/src/deploy.rs`, and `crates/wit_interfaces/wit/supervisor/supervisor.wit`, against the house rule in [AGENTS.md](../../AGENTS.md#ai-agent-guidelines) -- these plans get archived and renumbered, and the comment then points at nothing. B2 writes none and cleans only the lines it edits; a forty-site sweep of another slice's files does not belong in a slice that is also changing behavior. | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.15. **Pickup trigger:** the next non-behavioral pass over those files, or M05B closeout, whichever comes first |
| **The HTTP bridge has no idempotency path of its own (M05B B2, planned)** | B2's receiver-side dedup guard reads the JSON-RPC *body*, and both HTTP-bridge paths (`handle_json_rpc_bridge`, `dispatch_native`) forward into the guarded `dispatch_json_rpc_once` -- so an external HTTP client that puts `idempotency_key` in its body already gets guest semantics, including the fail-closed refusal. **Corrected after review: this is not a structural exclusion, as an earlier version of this row claimed.** What is missing is an HTTP-native way to express the key (a header, a documented idempotent-POST contract) and an outbox for a client that has no service on this node -- a separate ingress design with no consumer yet. | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.6, D-B2-7; `crates/router/src/route_handler/http.rs:185,438`. **Pickup trigger:** an external client needs safe retries against a substrate |
| **A service with no unexpired instance certificate cannot use the durable call path (M05B B2, planned)** | B2's `enqueue` refuses when the calling service holds no unexpired instance certificate (D-B2-20): without one the proxy presents no identity, the receiver builds an anonymous caller, and an anonymous dedup namespace is shared -- two callers using the same key string would read each other's stored results. Refusing at enqueue is better than queuing a call every delivery attempt would refuse until it dead-letters hours later. Ordinarily invisible, since the supervisor renews certificates; on a node running deployed services with no supervisor, a service can sit in this state permanently. | TBD | [M05B slice B2 plan](./milestones/M05B-async-primitives/slice-b2-implementation-plan.md) §0.16, D-B2-20; `crates/router/src/proxy.rs` (`invoke_remote_at`'s guest arm). **Pickup trigger:** a deployment runs deployed services with no certificate-renewing supervisor, or an offline-first identity story makes a durable call possible without a certificate |

## 9. Observability

| Item | What's deferred / why | Target | Source of record |
|---|---|---|---|
| Observability engine build-out | Current engine is a placeholder / basic shell. | TBD | `observability/src/engine.rs` |
| Process-global tracing init | Handle process-global tracing initialization more cleanly. | TBD | `observability/src/engine.rs` |
| Resource metering `[FND-OBS]` | Exact per-service utilization metering (for billing) not built. | TBD | [system-requirements-spec.md](../system-requirements-spec.md) feature matrix |
| No queryable `DecisionTrace` API | ADR-0017 §9's per-decision trace (asserter DID, `valid_until_secs`, compiled predicate, etc.) is only observable via `tracing` (`DecisionTrace::emit`) -- there's no API response field or query surface for it yet. Confirmed at unit level (`fdae_fetch`/`finalize` tests) but `federated_fdae_e2e.rs` can't assert a successful fetch's provenance end to end without capturing log output, which the test deliberately doesn't do. | Later slice | `crates/fdae/src/trace.rs`; `crates/substrate/tests/federated_fdae_e2e.rs` |

## 10. Product surfaces & UX

| Item | What's deferred / why | Target | Source of record |
|---|---|---|---|
| Consent / moderation UX `[PRD-SAF]` | Retargeted to a consumer-facing surface (Chat/Hub) that doesn't exist yet. | M06+ | [traceability-matrix.md](./traceability-matrix.md) |
| Syneroym Hub UI, marketplace/aggregator/facilitator `SynSvcs`, Producer-Distributor mesh | Phase 6 product expansion. | Phase 6 | [meta-implementation-plan.md](./meta-implementation-plan.md#later-phase-additions) |
| Accessibility (WCAG 2.1 AA) & localisation/i18n | Architecture to stay i18n-ready; full support later. | Later phase | [system-requirements-spec.md](../system-requirements-spec.md#appendix-later-phase-additions) |
| Mobile secure-hardware bindings (StrongBox / Secure Enclave), mobile lifecycle | Part of the mobile milestone. | Mobile milestone | [meta-implementation-plan.md](./meta-implementation-plan.md) |
| **Endpoint-record metadata (nickname, privacy) travels inside a signed record it no longer needs** | With an instance certificate installed, the stored `registry_certificate` blob exists only to carry two operator-chosen fields, and on the `--instance-certificate` deploy path it is signed by a throwaway key and cannot self-verify at all. The clean shape is `DeployManifest` fields, not taken in A1: the cost is a WIT record change, ~40 `DeployManifest` literals, **and** a new per-service persistence sidecar, since `EndpointPublisher` reads this at heartbeat time and a manifest is not stored per service today (`hosted_apps/<id>.json` is currently doing that job). | TBD | [M05A slice A1 plan](./milestones/M05A-app-supervisor/slice-a1-implementation-plan.md) D-A1-4/D-A1-8; `crates/core/src/endpoint_publisher.rs`; `apps/roymctl/src/commands/svc.rs` |
| **`roymctl svc deploy --volume` cannot materialize files into a container volume** | `ContainerVolumeMapping.files` (writing a generated config file into a volume before the container starts) has no `--volume`-adjacent CLI flag -- `parse_container_volume_mapping` always sets `files: vec![]`. Still reachable via a raw `deploy` JSON-RPC call or a `SynApp` manifest's `files = [...]` list, just not from a bare `svc deploy` invocation. Needs its own repeatable-value syntax distinct from `--volume`'s own (e.g. `--volume-file <host_path>:<relative_path>:<content>`), deferred as a separate, smaller CLI addition rather than folding it into this pass. | TBD | `apps/roymctl/src/commands/svc.rs` (`parse_container_volume_mapping`); [developer-guide.md](../developer-guide.md#mounting-configuration-files-into-a-container) |

## 11. Open in-code markers

Live `TODO`/`FIXME` markers that encode a real deferral. Remove both the code marker **and** its row here when resolved. (Excludes markers already resolved in-tree, e.g. the former `TODO(M4)` init-context gates removed in B0 and the `session.rs` `TODO(B7)` resolved in B7b.)

| Location | Marker | Maps to |
|---|---|---|
| `crates/app_orchestration/src/compiler.rs:163` | Temporary M1 hack force-prepending in the deployment-plan compiler | §8. **Survives [M05A slice A0](./milestones/M05A-app-supervisor/slice-a0-implementation-plan.md)**: A0 substitutes real member master DIDs into `PlannedService.service_id`/`resolved_dependencies` *after* `compile()` returns (D-A0-7), keeping `compile()` pure, so the fabricated no-private-key DID remains the plan's internal graph key and stays the actual deployed id on the no-master path |
| `crates/client_gateway/src/gateway.rs:34` | Present substrate-owner (controller) DID as caller | §7. **Narrowed by [M05A slice P0](./milestones/M05A-app-supervisor/slice-p0-implementation-plan.md)** (2026-07-30): `ControllerAgreement` now exists, but that's a mutual attestation, not a delegation the node can present on the wire — provisioning a real owner-signed delegation for this purpose is still not built. |
| `crates/client_gateway/src/gateway.rs:108` | Local-machine-only security stopgap | §7 |
| `crates/observability/src/engine.rs:6` | Engine is a placeholder/basic shell | §9 |
| `crates/observability/src/engine.rs:52,70` | Cleaner process-global tracing init | §9 |
| `crates/sdk/src/lib.rs:60` | Self-asserted pubkey is an assertion, not FDAE-backed proof | §3 |
| `crates/ucan/src/capability.rs:392` | Capability passthrough semantics documented but not implemented | §3 |
| `crates/router/src/proxy.rs:192` | Coarse interim fail-closed dispatch gate (stands in for FDAE) | §3 |
| `crates/router/src/route_handler/io.rs:147`, `route_handler/dispatch.rs:84` | Fine-grained caller authorization for the five data native-capability interfaces. **Narrowed by [M05A slice P0](./milestones/M05A-app-supervisor/slice-p0-implementation-plan.md)** (2026-07-30): `security` is gated on `substrate/admin` now too, alongside `orchestrator` (M04A Slice B7b); only the five data interfaces remain ungated here. | §3 |
| `crates/router/src/route_handler/http.rs:930` | Blob GET authorization is HMAC-only pending FDAE | §3 |
| `crates/router/src/lib.rs:4`, `preamble.rs:19,28,29,140`, `route_handler.rs:4` | wRPC wire formats not implemented | §1 |
| `crates/sandbox_wasm/src/engine.rs:533` | `migrate()` snapshot/rollback safety net | §2 |
| `crates/sandbox_wasm/src/engine.rs:599` | Cache function-parameter details (perf) | §2 |
| `apps/roymctl/tests/cli_args.rs:10` | Expand CLI argument-parsing tests | (test coverage) |
| `conversions.rs` (data layer) | Positional→named param binding | §2 |

---

## Recently resolved

Move items here (with commit/PR) when they land, then prune periodically.

| Item | Resolved by |
|---|---|
| **App Supervisor: durable push delivery (outbox/DLQ)** | [M05B slice B1](./milestones/M05B-async-primitives/slice-b1-implementation-plan.md), 2026-08-05: `syneroym-async-queue` (outbox + dead_letters, claim/complete/fail/replay) backs a durable `SubstrateActor` (`syneroym-sdk::deploy::build_durable_actor`) whose `write_bindings` attempts synchronously first and enqueues only on a transport failure, distinguished from a callee error via `JsonRpcError` downcasting. `apply_plan`/`restart`/`renew_cert` stay undurable (D-B1-3/D-B1-12: the latter two embed a certificate that expires in hours). A worker spawned beside the resident loop claims due items, takes the same `instance_lock` a live pass holds (D-B1-14), and applies the applied/no-op/stale/conflict/transport-error outcome mapping (D-B1-15). Terminal failures raise a standing `AlertKind::DeliveryExhausted` per `(instance, logical_ref, substrate)` with the dead-letter count in `detail`, cleared when the last one for that key is replayed away; `supervisor.wit` gained `dead-letters`/`replay`, and `roymctl supervisor dead-letters`/`replay`. This is A6, executed as B1. **A post-landing review (2026-08-05) found and fixed sixteen further gaps against the shipped code**: an epoch-vs-outbox skew that could strand convergence (`push_bindings` now defers to an already-pending queue row exactly as `enqueue_unreachable_push` already did); `replay` bypassing the one-row-per-key invariant (now refuses when a pending row already exists); an unnotified DLQ prune leaving `DeliveryExhausted` alerts nothing could clear, and the cap itself being global instead of per-instance (both fixed via a caller-supplied `group_key` the queue crate stores but never parses); the queue worker not actually racing cancellation against a delivery in flight (see the shutdown-latency row above); a claimed item that panics before `fail` never counting against its attempt budget (closed via `claim_count`, a second, claim-side counter); `queue_max_attempts = 0` silently disabling retry; every reached-and-answered write-bindings failure being treated as the row-9 "target gone" case (narrowed to the one message `write_bindings_impl` actually uses for that, `syneroym_sdk::deploy::is_target_gone_error`); `outbox.rs`'s dedup check failing open on a broken read instead of closed; `replay`'s error codes for a caller's own mistakes; a retired instance's still-queued item being delivered or noisily dead-lettered instead of quietly dropped; and the reference scenario's own steps 4/5/7 having no RPC surface to assert the outbox directly (`supervisor.wit` gained `outbox`, `roymctl supervisor outbox`). |
| **A4: `deployed_service_id` assumes member index 0** | [M05A Slice A5e](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5e-5/D-A5e-11: `deployed_service_id`, `master_for_member`, `mint_and_substitute`, and `substitute_and_certify_members` all read the member's own stored index instead of the literal `0`. The row's own text predicted this ("A5's reference-scenario step 5 breaks this the moment it lands"); the resolution turned out larger than the row described, since the same index-0 assumption sat in three more call sites beyond the one the row named. |
| **No Criterion bench case pinning A2's "no network hop" dependency-resolution budget** | [M05A Slice A5e](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5e-13: `crates/app_orchestration/benches/resolver.rs`, not `crates/router/benches/proxy.rs` as this row's own text expected — `ProxyRouter` has no dependency target at all, since `CallTarget::Dependency` resolves in the WASM host capability before a `ProxyRequest` exists. Three cases (cache hit, cache miss through the registry, a two-member `Redundant` round-robin); measured at ~60 ns, ~306 ns, and ~58 ns respectively. |
| **A renewal that installs a certificate outside `deploy` must also refresh `SynSvcNativeService`** | [M05A Slice A5d](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5d-3: `renew_cert_impl` rebuilds the native-dispatch entry around the new certificate, mirroring `deploy_with_context`'s whole construction site (including `service_proxy`/`row_authorizer`) and re-reading the stored FDAE policy — which aborts the renewal if it no longer parses, rather than silently dropping enforcement. Proven by `renew_cert_rebuilds_syn_svc_native_service_with_the_new_certificate`, which signs a relationship proof afterwards and checks it verifies against the *new* certificate. |
| **No maximum lifetime enforced on an installed instance certificate** | [M05A Slice A5d](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5d-7, with the number recorded: **30 days**, enforced inside the shared `verify_installed_instance_cert` so `deploy` and `renew-cert` apply it identically. The row asked for a policy decision; the decision is that this is a *backstop*, not a forcing function — a tight cap would contradict ADR-0020 §3's attended posture, whose certificates are deliberately long-lived, so the ceiling is generous enough to catch an unbounded mint and nothing else. "Short-lived" is delivered separately, by the supervisor minting at `renewed_cert_expires_hours` (4h) and renewing unattended. |
| **No operator surface for revoking an instance key** | [M05A Slice A5d](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5d-10/D-A5d-15: `RegistryClient::revoke_instance_key` is the production writer the row said did not exist, and `roymctl supervisor revoke-instance` the operator surface. Closed end to end, not only for the loop: a `revoked_placements` row is read by `apply_with_clients`, so `submit` and `force-reconcile` stop re-certifying a revoked member too — without that half, the next ordinary resubmit silently reinstated the key. |
| **Master-anchor refresh is a read-modify-write with a race, and a daily operator duty nothing performs on a schedule** | [M05A Slice A5d](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5d-8, both halves. The schedule: the resident loop republishes each managed master's anchor when `master_anchor_refresh_interval_secs` (12h) has elapsed, evaluated on the ordinary pass tick against a persisted `(master_did, last_refreshed_at)` fact rather than a second timer. The race: closed by documenting mint-in-place's existing single-writer invariant rather than building a CAS the registry does not offer — the concurrent-custody case that *would* need one is now its own row above. **Confirmed by [M05B](./milestones/M05B-async-primitives/task.md) planning** (2026-08-04): that milestone builds no registry compare-and-set either, for the same reason, and its own scheduling work evaluates on the resident loop's existing tick — the shape this row's schedule half already uses. [ADR-0023](../decisions/0023-durable-async-primitives.md) §6. |
| **A3: `Degraded` has no automatic exit, and "keep retrying" means a manual re-run** | [M05A Slice A5c](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) phase 5: the resident loop's own healthy sweep clears a service's remediation state and its alerts once it lands, with no operator re-run needed. |
| **A3: `ActionState::Pending` still has no writer** | [M05A Slice A5c](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §24: re-argued rather than given a writer. With D-A5c-2 the loop applies a filtered plan through the existing `apply_plan`, which writes `InProgress` directly — there is still no `Pending` writer and never will be one under this design. The variant is removed (`crates/app_orchestration/src/journal.rs`) rather than left for a later slice to rediscover unused. |
| **`restart` is the only lifecycle write into a deployed service with no service-owner check** | [M05A Slice A5c](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5c-15: `restart_impl` gains an `owner_of` check with the same node-wide `ORCHESTRATOR_DEPLOY` override `undeploy_impl` already has. |
| **The supervisor's `submit`/`force-reconcile` never skip an already-landed service, so every call resends every artifact** | [M05A Slice A5c](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5c-2: resolved by the resident loop's filtered plan (`Reconciler::compute_diff`'s Add/Update actions plus `missing_placement`), not by re-scoping `apply_plan`'s own skip check — the caller audit in the plan's §19.2 found re-scoping it would silently break `roymctl app deploy`'s own "redeploy to repair" behavior against a healthy instance. `submit`/`force-reconcile` themselves are unaffected; only the loop's own redeploy path is filtered. |
| **A5b: the supervisor applies a placement change silently, running two live copies of one member** | [M05A Slice A5c](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5c-1: `refuse_placement_change`, called from both `handle_submit`'s and `handle_force_reconcile`'s pre-flight. |
| **A5b: a partially-deployed app instance reports `Active`, so failure-matrix row 12's read surface is unmet** | [M05A Slice A5c](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5c-10: `overall_state` is `Degraded` when any planned service has no landed placement, not only when the health sweep reports a fault. |
| **A4: alerts are not published to MQTT** | [M05A Slice A5c](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5c-6: the supervisor's `messaging` registration and `publish_opened_alerts`, publishing every alert a pass newly opens under its own namespaced topic. |
| **A4: a wasm `rpc` probe costs a component instantiation** | [M05A Slice A5c](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) D-A5c-12: resolved and retargeted with a number — a 20-service steady-state pass under 2s and at most 2 RPCs per substrate, measured by a budget harness before the loop was written. The measurement surfaced a real pool-exhaustion bug under concurrent probes (see the wasmtime row below), fixed with a semaphore (`AppSandboxEngine::probe_instance_permits`). |
| **Master-DID endpoint resolution requires an HTTP registry** | [ADR-0020](../decisions/0020-stable-logical-service-identity.md) §6's revision, 2026-07-29: the endpoint record is now signed by the deployer's own master key directly, not by a delegated instance key, so the signing key and the record's key are always the same and every record has a DHT home. `RegistryClient::register`'s HTTP-registry-required refusal for a delegation-signed record is deleted. |
| **Endpoint records are not revocation-checked at resolution** | Reworded rather than resolved by the same revision: with no certificate on a record at all, there is nothing there to revocation-check — the property was always accurate for what it actually described (revocation is a handshake-only concern), and is no longer stated as a backlog gap. |
| **The DHT endpoint-record path has its own delegation check, and ADR-0020 §6 names only the HTTP one** | [M05A slice A1](./milestones/M05A-app-supervisor/slice-a1-implementation-plan.md) D-A1-1: there is one verification function (`SignedEndpointInfo::verify`), not two — `verify_endpoint_signature` merely calls it. A1 rewrote it to ADR-0020 §6's keying (record keyed by the master, signed by the instance key), replacing A0's inverse placeholder check. |
| `anchor_did` anchor stamp (ADR-0015 A5) | `5a6f047`, remote fetch `279d284` (#89) |
| `TODO(M4)` init-context DDL gates → `data-layer/admin` capability gate | Slice B0 |
| `session.rs TODO(B7)` facts gate | Slice B7b (ADR-0015 A6) |
| Federated cross-service parameter fetch (pipeline stage 2) | M04B Slice B3 Phases 1-4 (#89, #100, Phase 4) |
| D-04-02-h guest-originated-read identity threading (both ingresses) | M04B Slice B3.5-fdae, 2026-07-25 |
| FDAE stage-4 WASM ABAC | M04B Slice B4-fdae, 2026-07-25 |
| FDAE write-side Mode-A authorization (D-04-02-f) | M04B Slice B5-fdae, 2026-07-26 |
| Self-proxy write attribution disagrees with the direct-WIT write path | M04B Slice B5-fdae, 2026-07-26 (D-B5-5, `CallerContext::write_attribution`) |
| `drop-collection` carried no `data-layer/admin` capability gate while `execute-ddl`/`query-raw` did | Post-review, 2026-07-27: gated identically in both ingresses (`sandbox_wasm/src/host_capabilities.rs`, `control_plane/src/synsvc_native.rs`) |
| **A service's signing identity is still its instance key, so `expected_asserter_did` does not survive reinstantiation** | [M05A Slice A2](./milestones/M05A-app-supervisor/slice-a2-implementation-plan.md), 2026-07-29: `RelationshipProof` gains an optional `delegation` field (a JSON `DelegationCertificate`); `sign` asserts under the certificate's master when one is given, `verify` accepts an instance-key signature carrying a certificate from the policy-declared master (D-A2-11/D-A2-12). `ProxyRouter::invoke_remote_at`'s `(None, CallOrigin::Native { service_id: Some(_) })` arm now presents the service's own certified instance key, the transport half of the same gap (D-A2-13). Failure-matrix row 19 proven live in `crates/router/src/proxy.rs` (`a_native_origin_call_on_a_services_behalf_presents_that_services_instance_key` and neighbors) and `crates/rpc/src/relationship_proof.rs`. |
| **A3: substrate capabilities are operator-declared, not probed** | [M05A Slice A4](./milestones/M05A-app-supervisor/slice-a4-implementation-plan.md), D-A4-15/§7.3: `status` now reports `node-facts.service-types` (`cfg!(feature = …)`), and `roymctl app deploy`'s preflight compares it against the inventory's declared `capabilities`, warning on a mismatch. Skipped, with an explicit note, for a credential without node-wide `orchestrator/status` (D-A4-18). |
| **A3: multi-substrate placement requires one registry namespace, verified only after the fact** | [M05A Slice A4](./milestones/M05A-app-supervisor/slice-a4-implementation-plan.md), D-A4-15/§7.3: `status` now reports `node-facts.registry-url`/`dht-enabled`, and `roymctl app deploy`'s preflight refuses outright, before any artifact work, when every placed alias's credential can read node facts and they disagree with no DHT fallback. `probe_registry_reachability` stays as the propagation check and the fallback for a credential that cannot read node facts. |
| `ControllerAgreement` creation tool | [M05A Slice P0](./milestones/M05A-app-supervisor/slice-p0-implementation-plan.md), 2026-07-30: `roymctl substrate claim` mints a mutually-signed `ControllerAgreement` from two local key files; the `security` interface is gated on `substrate/admin`; `build_caller`'s unowned-substrate bootstrap grant is removed, so an unowned substrate fails closed instead. Was M5 item 5, pulled forward into M05A as Slice P0 on 2026-07-27 (see the row this replaces for why it had drifted). |
| `resolve_relation`'s A1/A2 fork is defeated on an unowned substrate | [M05A Slice P0](./milestones/M05A-app-supervisor/slice-p0-implementation-plan.md), 2026-07-30: the free, bare `substrate:<node_did>`-scoped grant every verified caller used to receive on an unowned substrate is gone — an unowned substrate now grants no node-wide capability at all, so `resolve_relation`'s B3-07 fork no longer sees a false-positive substrate-scoped capability to route on. The workaround comment in `federated_fdae_e2e.rs` explaining why Node A had to be owned to avoid this is retired (Node A still needs an owner post-P0, but for the unrelated reason that every substrate now fails closed without one). |
| **Container/podman deploys cannot carry a member master** | 2026-07-30: `roymctl svc deploy` gained `--image`/`--port`/`--volume` (`apps/roymctl/src/commands/svc.rs`), and `SyneroymClient::deploy_container` now takes `instance_certificate: Option<DelegationCertificate>` matching `deploy_svc_wasm`/`deploy_svc_tcp`, so `--master`/`--instance-certificate` work identically for a container deploy. |
| **`app_instance_owners` rows never get forgotten** | [M05A Slice A5a](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §5.2, D-A5-6: `app_instance_owners` is dropped outright and replaced by `app_instance_management`, keyed the same way but carrying an owner + monotonic generation stamp that `write-bindings`/`restart`/`claim-app-instance` gate on, so a stale row is superseded by a higher generation instead of accumulating forever. No migration, per pre-release policy — tracked as its own row below. |
| **A2's binding write is last-write-wins, no epoch guard** | [M05A Slice A5a](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §3: `classify_binding_write` (`crates/app_orchestration/src/resolver.rs`) implements ADR-0021 §3's four-case epoch rule (Applied/NoOp/Conflict/Stale) as a pure function, and `write_bindings_impl` enforces it before any `StaticInventory`/`EndpointStorage` write lands, closing the race a concurrent membership change used to hit. |
| **No binding-only write path — A5 cannot push a membership change without restarting every dependent** | [M05A Slice A5a](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §3, §5.7: the new `write-bindings` verb (`orchestration.rs`'s `write_bindings_impl`, dispatched via `service.rs`) lets a caller push an epoch-guarded binding update straight into a running dependent's `StaticInventory`, with no restart — proven live in `crates/substrate/tests/binding_push_e2e.rs`'s `a_membership_change_pushed_to_a_dependent_takes_effect_without_a_redeploy` **for the persisted, per-dependent `status.binding-epochs` row**; a live wasm guest's own next `dependency(...)` call resolving to the new member is not proven live (see the row below). |
| **`roymctl svc start`/`svc stop` call orchestrator methods that do not exist** | [M05A Slice A5a](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §4: `SvcCommands::Start`/`Stop` are replaced by `SvcCommands::Restart`, backed by a real `restart` verb (`restart_impl`) that reloads a wasm component or restarts a container in place. |
| **A4: `status` reports no binding state** | [M05A Slice A5a](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §6: `service-status` gained `binding-epochs: list<tuple<string, u64>>`, populated per dependency name in `service_status_for`, so an operator can see the epoch a running dependent currently serves for each binding without a live guest call. |
| **A3: failure-matrix row 10 (lost-response redeploy) is unmet** | [M05A Slice A5a](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §4A: `deploy_with_context` hashes the manifest content (blake3) and short-circuits to `Ok(())` on an identical redeploy by the *same* caller (or when no owner is yet set), without re-running the deploy side effects — closing the retry-after-lost-response gap while still transferring ownership when a different caller redeploys byte-identical content. |
| **`app deploy` without `--mint-masters` binds nothing** | [M05A Slice A5a](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §4.5: `refuse_unmastered_dependencies` turns the old warning into a hard refusal — `roymctl app deploy` now errors out before deploying anything when a manifest declares `depends_on` but was not mastered, instead of silently deploying a service with no bindings. |
| **A supervisor needing vault writes must hold `substrate/admin` on every managed substrate** | [M05A Slice A5b](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §11.4: resolved by the custody design itself, not by building the per-service `set-secret` gate P0 deferred. `MasterVault` opens the supervisor's *own* node's service database through `StorageProvider::open_service_db`, an in-process call — the supervisor never issues a remote `security/set-secret` against a managed substrate at all, so it never needs `substrate/admin` there. The underlying P0 ask (a per-service `set-secret` gate at `substrate:<node>/app/<svc>`) stays open as its own row below, retargeted to whenever supervisor-provisioned secrets for *managed* services (not the supervisor's own vault) arrive — which A5b does not do. |
| **A3: `apply_plan`'s future is not `Send`, which A5's supervisor loop will need** | [M05A Slice A5b](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §10.4, D-A5-8: `DeploymentJournal` and `AlertStore` both changed `conn: Connection` → `conn: Arc<Mutex<Connection>>`, with a `from_connection` constructor so `SupervisorStore` backs its desired-state table, the journal, and alerts with one shared connection. `apply_plan_returns_a_send_future` (`crates/sdk/src/deploy.rs`) and `journal_and_alert_store_are_send_and_sync`/`a_journal_reference_held_across_an_await_is_send` (`crates/app_orchestration/src/journal.rs`) pin it. |
| **A4: the alert store is a local file A5 cannot read** | [M05A Slice A5b](./milestones/M05A-app-supervisor/slice-a5-implementation-plan.md) §10.3: `SupervisorStore` constructs its own `AlertStore` against `supervisor.db` (via `AlertStore::from_connection`), a substrate-side database the supervisor's `alerts` RPC verb serves directly — no client-side file involved. The A4-era `alerts.db` is unrelated and unmigrated, per pre-release policy. |
