[package]
name = "syneroym-coordinator-webrtc"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

# `askama_axum` is referenced only by code the `#[derive(Template)]` macro
# generates (the `IntoResponse` impl from askama's `with-axum` feature), so
# cargo-shear cannot see the use and would strip it.
[package.metadata.cargo-shear]
ignored = ["askama_axum"]

[dependencies]
syneroym-core.workspace = true
syneroym-data-db.workspace = true
syneroym-router.workspace = true
syneroym-identity.workspace = true
syneroym-sdk.workspace = true
syneroym-app-orchestration.workspace = true
anyhow.workspace = true
async-trait.workspace = true
tokio.workspace = true
tracing.workspace = true
axum.workspace = true
tower.workspace = true
tower-http.workspace = true
askama.workspace = true
askama_axum.workspace = true
serde_json.workspace = true
futures.workspace = true
iroh.workspace = true
bytes.workspace = true
z32.workspace = true
ed25519-dalek.workspace = true
hex.workspace = true
flate2.workspace = true

[dev-dependencies]
tempfile.workspace = true
syneroym-community-registry.workspace = true
tower.workspace = true
http-body-util.workspace = true

[build-dependencies]
swc_core = { version = "76.0.0", features = [
    "common",
    "ecma_parser",
    "ecma_codegen",
    "ecma_minifier",
    "ecma_visit",
    "ecma_ast",
    "ecma_transforms",
] }
